// Range expressions build lists of Ints:
xs = 1..3

// Inside list literals, range items splice their elements into the list:
ys = [0, 1..3, 4]
zs = [1..3]

// `...` is an explicit spread (splice):
ws = [0, ...(1..3), 4]
