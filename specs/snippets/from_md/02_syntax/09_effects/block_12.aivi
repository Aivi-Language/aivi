// Concise (recommended in effect blocks)
main = do Effect {
  res <- attempt (foo x)
  verdict = res match
  | Ok _  => "ok"
  | Err _ => "err"

  print verdict

  if cond then print "branch" else Unit
}
