// Concise (recommended in effect blocks)
main = effect {
  res <- attempt (foo x)
  verdict = res ?
    | Ok _  => "ok"
    | Err _ => "err"

  print verdict

  if cond then print "branch" else Unit
}
