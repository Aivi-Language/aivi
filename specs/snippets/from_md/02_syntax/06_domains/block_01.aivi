// Bring Vector math into scope
use aivi.vector (domain Vector)

position = { x: 10.0, y: 20.0 }
velocity = { x: 1.0, y: 0.0 }

// The '+' operator now knows how to add `Vec2` records as vectors
new_pos = position + velocity
