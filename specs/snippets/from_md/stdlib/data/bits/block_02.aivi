use aivi.bits

extractRgb : Bits -> { r: Int, g: Int, b: Int }
extractRgb = color => mask = fromInt 0xFF {
  r: color |> shiftRight 16 |> and mask |> toInt
  g: color |> shiftRight 8 |> and mask |> toInt
  b: color |> and mask |> toInt
}
