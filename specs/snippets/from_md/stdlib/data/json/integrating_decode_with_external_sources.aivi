// Source definition
users : Source File (List User)
users = file.json "./users.json"

// Using it in a do block
loadUsers = do Effect {
  // If `./users.json` contains malformed entities, the Effect short-circuits
  // and yields a `SourceError.DecodeError(errs)`!
  data <- users

  // Here `data` is guaranteed to be a valid `List User`
  pure data
}
