use aivi.signal (fft)

// A simple time-domain signal (e.g., audio samples)
timeDomain = { samples: [1.0, 0.5, 0.25, 0.125], rate: 44100.0 }

// Convert to frequencies to analyze pitch
freqDomain = fft timeDomain
