usersWithRecentOrders =
  users[active]
    |> selectMap {
         id: .id
         email: .email
         orders: (
           .orders[status == "paid"]
             |> orderBy (desc .createdAt)
             |> limit 3
             |> selectMap { id: .id, total: .total }
         )
       }
