conn <- db.connect { driver: Sqlite, url: ":memory:" }
_ <- db.runMigrationsOn conn [users]
_ <- db.insertOn conn users { id: 1, email: "ada@example.com", active: True }
activeUsers <- db.rowsOn conn users[active]
_ <- db.close conn
pure activeUsers
