salesByUser =
  orders[status == "paid"]
    |> groupBy .userId
    |> having (sum .total > 100)
    |> selectMap {
         userId: key
         orderCount: count
         totalSpent: sum .total
       }
    |> orderBy (desc .totalSpent)
