connectLocal = db.configure { driver: Sqlite, url: ":memory:" }

applySchema = db.runMigrations [users]

seedAda = db.insert users { id: 1, email: "ada@example.com", active: True }

activeUsers = db.rows users[active]

firstUser = db.first users[id == 1]
