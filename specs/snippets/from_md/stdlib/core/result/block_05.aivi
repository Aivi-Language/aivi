use aivi.logic
use aivi.result

defaultUser = { name: "Guest", role: "viewer" }

validateUser = input => parseJson input
  |> mapErr (e => { field: "body", error: e })
  |> chain validateSchema
  |> chain checkPermissions
  |> map normalizeData

userData = validateUser rawInput |> getOrElse defaultUser
