use aivi.i18n

loadBundle : Locale -> Text -> Effect Text Bundle
loadBundle = locale path =>
  bundleFromPropertiesFile locale path
|> bundleResult => bundleResult match
  | Ok bundle => pure bundle
  | Err e     => fail e
