loadTranslator : Locale -> Text -> Effect Text (Key -> {} -> Text)
loadTranslator = locale path =>
  bundleFromPropertiesFile locale path
|> bundleResult => bundleResult match
  | Ok bundle => pure (t bundle)
  | Err e     => fail e
