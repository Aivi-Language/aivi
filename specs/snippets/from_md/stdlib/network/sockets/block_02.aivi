use aivi.net.sockets

pingServer = do Effect {
  conn  <- connect { host: "127.0.0.1", port: 9000 }
  _     <- send conn [112, 105, 110, 103]
  reply <- recv conn
  _     <- close conn
  pure reply
}
