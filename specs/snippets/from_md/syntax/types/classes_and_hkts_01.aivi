class Functor (F A) = {
  map: (A -> B) -> F B
}

// Tokens explained:
// - Functor: The class name
// - F: Generic type constructor parameter
// - A: Implicitly universally quantified type variable
// - Use `given (A: ClassName)` only when a real constraint is needed
