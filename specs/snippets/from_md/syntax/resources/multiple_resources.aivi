compareFiles = leftPath rightPath => do Effect {
  left  <- managedFile leftPath
  right <- managedFile rightPath

  leftContent  <- file.readAll left
  rightContent <- file.readAll right
  print "{leftContent} / {rightContent}"
}
