// Attempt an operation, providing a typed default on error
getUser = id =>
  api.fetchUser id
|> attempt
|> res => res match
  | Ok user => user
  | Err _   => GuestUser

validatedUser = getUser 123
  |> u => if u.age > 18 then
    pure (toAdmin u)
  else
    fail TooYoung
