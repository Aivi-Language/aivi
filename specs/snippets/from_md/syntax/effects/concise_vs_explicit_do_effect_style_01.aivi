showVerdict = verdict =>
  print verdict
  ~|>if cond then print "branch" else pure Unit

// Concise (recommended in flat flows)
main = foo x
  |> attempt
  |> result => result match
    | Ok _  => "ok"
    | Err _ => "err"
  |> showVerdict
