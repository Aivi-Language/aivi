resultToVerdict = result => result match
  | Ok _  => "ok"
  | Err _ => "err"

showVerdict = verdict =>
  print verdict
  ~|>if cond then print "branch" else pure Unit

// More explicit (same semantics)
main = foo x
  |> attempt
  |> resultToVerdict
  |> showVerdict
