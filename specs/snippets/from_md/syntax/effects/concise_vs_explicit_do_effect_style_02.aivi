// More explicit (same semantics)
main = do Effect {
  res <- attempt (foo x)
  verdict = res match
    | Ok _  => "ok"
    | Err _ => "err"

    _ <- print verdict

    _ <- if cond then print "branch" else pure Unit
}
