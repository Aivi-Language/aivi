@test
module order_tests

use aivi
use aivi.testing

add = left right => left + right

@test "addition is commutative"
addCommutative = assertEq (add 2 3) (add 3 2)

@test "empty list has length zero"
emptyLen = assertEq (length []) 0
