use aivi.mutableMap

snapshotUsers : Effect e (Map Text Int)
snapshotUsers =
  do Effect {
    m <- MutableMap.empty Unit
    _ <- MutableMap.insert "alice" 1 m
    _ <- MutableMap.insert "bob" 2 m
    MutableMap.freeze m
  }
