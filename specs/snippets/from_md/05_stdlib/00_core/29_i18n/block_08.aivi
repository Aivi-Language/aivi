use aivi.i18n

loadBundle : Locale -> Text -> Effect Text Bundle
loadBundle = locale path => do Effect {
  res <- bundleFromPropertiesFile locale path
  res match
  | Ok b  => pure b
  | Err e => fail e
}
