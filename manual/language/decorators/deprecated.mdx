---
title: "`@deprecated` — Deprecation Warnings"
description: "@deprecated marks a binding as deprecated. The compiler emits a warning at every call site with the provided migration hint."
---
<!-- quick-info: {"kind":"decorator","name":"@deprecated"} -->
`@deprecated` marks a binding as deprecated. The compiler emits a warning at every call site with the provided migration hint.
<!-- /quick-info -->

## Syntax

```aivi
@deprecated "migration hint message"
binding = ...
```

## Example

```aivi
@deprecated "use `windowSetTitle` instead"
setTitle : WindowId -> Text -> Effect GtkError Unit
setTitle = windowSetTitle

// Calling setTitle anywhere now emits:
//   warning: `setTitle` is deprecated – use `windowSetTitle` instead
```


The compiler emits a warning at every call site. Use a human-readable migration hint as the argument.
