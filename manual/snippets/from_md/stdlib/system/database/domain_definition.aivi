domain Database over Table A = {
  Delta = Delta A

  (+) : Table A -> Delta A -> Effect DbError (Table A)
  (+) = table delta => db.applyDelta table delta

  // Constructor aliases exposed by the domain.
  ins = Insert
  upd = Update
  del = Delete
  ups = Upsert
}
