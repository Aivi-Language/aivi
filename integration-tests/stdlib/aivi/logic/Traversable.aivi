@no_prelude
module integrationTests.stdlib.aivi.logic.Traversable

use aivi
use aivi.testing (assert)

use aivi.logic

subject = Traversable

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
