@no_prelude
module integrationTests.stdlib.aivi.logic.Bifunctor

use aivi
use aivi.testing (assert)

use aivi.logic

subject = Bifunctor

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
