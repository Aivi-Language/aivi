@no_prelude
module integrationTests.stdlib.aivi.logic.Foldable

use aivi
use aivi.testing (assert)

use aivi.logic

subject = Foldable

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
