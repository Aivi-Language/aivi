@no_prelude
module integrationTests.stdlib.aivi.logic.Semigroupoid

use aivi
use aivi.testing (assert)

use aivi.logic

subject = Semigroupoid

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
