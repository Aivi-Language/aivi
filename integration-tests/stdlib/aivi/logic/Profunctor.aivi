@no_prelude
module integrationTests.stdlib.aivi.logic.Profunctor

use aivi
use aivi.testing (assert)

use aivi.logic

subject = Profunctor

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
