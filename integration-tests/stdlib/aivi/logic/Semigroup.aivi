@no_prelude
module integrationTests.stdlib.aivi.logic.Semigroup

use aivi
use aivi.testing (assert)

use aivi.logic

subject = Semigroup

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
