@no_prelude
module integrationTests.stdlib.aivi.SourceError

use aivi
use aivi.testing
use aivi

touch : SourceError -> Unit
touch = _ => Unit

@test
smoke = effect {
  pure touch
  assertEq (1 + 1) 2
}
