@no_prelude
module integrationTests.stdlib.aivi.duration.Span

use aivi
use aivi.testing
use aivi.duration


touch : Span -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
