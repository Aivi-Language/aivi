@no_prelude
module integrationTests.stdlib.aivi.duration.negateDelta

use aivi
use aivi.testing (assert)

use aivi.duration

subject = negateDelta

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
