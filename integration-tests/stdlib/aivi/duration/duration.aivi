@no_prelude
module integrationTests.stdlib.aivi.duration.tests

use aivi
use aivi.testing
use aivi.duration

span_touch : Span -> Unit
span_touch = _ => Unit

@test
span_smoke = effect {
  pure span_touch
  assertEq (1 + 1) 2
}

@test
negateDelta_smoke = effect {
  assert (negateDelta 1s == -1s)
}
