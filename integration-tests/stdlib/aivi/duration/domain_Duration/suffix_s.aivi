@no_prelude
module integrationTests.stdlib.aivi.duration.domain_Duration.suffix_s

use aivi
use aivi.testing
use aivi.duration
use aivi.duration

subject = 1s

@test
smoke = effect {
  _ <- pure suffix_s
  _ <- assert True
}
