@no_prelude
module integrationTests.stdlib.aivi.duration.domain_Duration.suffix_h

use aivi
use aivi.testing
use aivi.duration
use aivi.duration

subject = 1h

@test
smoke = effect {
  _ <- pure suffix_h
  _ <- assert True
}
