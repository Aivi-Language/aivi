@no_prelude
module integrationTests.stdlib.aivi.duration.domain_Duration.suffix_min

use aivi
use aivi.testing
use aivi.duration
use aivi.duration

subject = 1min

@test
smoke = effect {
  _ <- pure suffix_min
  _ <- assert True
}
