@no_prelude
module integrationTests.stdlib.aivi.duration.domain_Duration.tests

use aivi
use aivi.testing
use aivi.duration
use aivi.duration (domain Duration)

@test "literals"
literals_smoke = do Effect {
  _ms <- 1ms
  _s <- 1s
  _m <- 1min
  assert True
}
