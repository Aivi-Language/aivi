@no_prelude
module integrationTests.stdlib.aivi.duration.domain_Duration.suffix_ms

use aivi
use aivi.testing (assert)

use aivi.duration

use aivi.duration (domain Duration)

subject = 2ms

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
