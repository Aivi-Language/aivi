@no_prelude
module integrationTests.stdlib.aivi.duration.domain_Duration.suffix_ms

use aivi
use aivi.testing
use aivi.duration
use aivi.duration

subject = 1ms

@test
smoke = effect {
  _ <- pure suffix_ms
  _ <- assert True
}
