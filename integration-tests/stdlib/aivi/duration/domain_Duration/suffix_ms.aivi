@no_prelude
module integrationTests.stdlib.aivi.duration.domain_Duration.suffix_ms

use aivi
use aivi.testing
use aivi.duration
use aivi.duration

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
