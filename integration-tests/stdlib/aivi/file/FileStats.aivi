@no_prelude
module integrationTests.stdlib.aivi.file.FileStats

use aivi
use aivi.testing
use aivi.file

touch : FileStats -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
