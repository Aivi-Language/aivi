@no_prelude
module integrationTests.stdlib.aivi.file.writeText

use aivi
use aivi.testing (assert)

use aivi.file

subject = writeText

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
