@no_prelude
module integrationTests.stdlib.aivi.Float

use aivi
use aivi.testing
use aivi

@test
smoke = effect {
  assert (1.0 + 2.0 == 3.0)
  assert (2.0 * 3.0 == 6.0)
  assert (4.0 / 2.0 == 2.0)
  assert (1.0 < 2.0)
  assert (2.0 > 1.0)
  assert (1.0 <= 1.0)
  assert (1.0 >= 1.0)
}
