@no_prelude
module integrationTests.stdlib.aivi.text.fromBytes

use aivi
use aivi.testing (assert)

use aivi.text

subject = fromBytes

@test
smoke = effect {
  // Inverse of toBytes
  _ <- assert True
}
