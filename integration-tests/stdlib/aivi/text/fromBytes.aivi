@no_prelude
module integrationTests.stdlib.aivi.text.fromBytes

use aivi
use aivi.testing (assert)

use aivi.text

subject = fromBytes

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
