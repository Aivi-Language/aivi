@no_prelude
module integrationTests.stdlib.aivi.text.repeat

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (repeat 3 "a" == "aaa")
  _ <- testing.assert (repeat 0 "a" == "")
}
