@no_prelude
module integrationTests.stdlib.aivi.text.repeat

use aivi
use aivi.testing (assert)

use aivi.text

subject = repeat

@test
smoke = effect {
  _ <- assert (repeat 3 "a" == "aaa")
  _ <- assert (repeat 0 "a" == "")
}
