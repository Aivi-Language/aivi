@no_prelude
module integrationTests.stdlib.aivi.text.Utf32

use aivi
use aivi.testing
use aivi.text


touch : Utf32 -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
