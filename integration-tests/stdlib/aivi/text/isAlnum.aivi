@no_prelude
module integrationTests.stdlib.aivi.text.isAlnum

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (isAlnum "a" == True)
  _ <- testing.assert (isAlnum "1" == True)
  _ <- testing.assert (isAlnum "!" == False)
}
