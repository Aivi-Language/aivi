@no_prelude
module integrationTests.stdlib.aivi.text.isAlnum

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- assert (isAlnum "a" == True)
  _ <- assert (isAlnum "1" == True)
  _ <- assert (isAlnum "!" == False)
}
