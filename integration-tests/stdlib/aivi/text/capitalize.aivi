@no_prelude
module integrationTests.stdlib.aivi.text.capitalize

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- assert (capitalize "hello" == "Hello")
  _ <- assert (capitalize "Hello" == "Hello")
}
