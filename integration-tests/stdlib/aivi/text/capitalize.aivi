@no_prelude
module integrationTests.stdlib.aivi.text.capitalize

use aivi
use aivi.testing (assert)

use aivi.text

subject = capitalize

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
