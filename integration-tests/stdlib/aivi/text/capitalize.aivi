@no_prelude
module integrationTests.stdlib.aivi.text.capitalize

use aivi
use aivi.testing

use aivi.text

subject = capitalize

@test
smoke = effect {
  _ <- testing.assert (capitalize "hello" == "Hello")
  _ <- testing.assert (capitalize "Hello" == "Hello")
}
