@no_prelude
module integrationTests.stdlib.aivi.text.compare

use aivi
use aivi.testing (assert)

use aivi.text

subject = compare

@test
smoke = effect {
  _ <- assert (compare "a" "b" < 0)
  _ <- assert (compare "b" "a" > 0)
  _ <- assert (compare "a" "a" == 0)
}
