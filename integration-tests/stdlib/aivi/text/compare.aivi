@no_prelude
module integrationTests.stdlib.aivi.text.compare

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (compare "a" "b" < 0)
  _ <- testing.assert (compare "b" "a" > 0)
  _ <- testing.assert (compare "a" "a" == 0)
}
