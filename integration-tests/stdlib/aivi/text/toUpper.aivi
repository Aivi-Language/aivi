@no_prelude
module integrationTests.stdlib.aivi.text.toUpper

use aivi
use aivi.testing (assert)

use aivi.text

subject = toUpper

@test
smoke = effect {
  _ <- assert (toUpper "hello" == "HELLO")
}
