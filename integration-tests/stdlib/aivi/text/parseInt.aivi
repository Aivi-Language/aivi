@no_prelude
module integrationTests.stdlib.aivi.text.parseInt

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (parseInt "123" == Some 123)
  _ <- testing.assert (parseInt "abc" == None)
}
