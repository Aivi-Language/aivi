@no_prelude
module integrationTests.stdlib.aivi.text.parseInt

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- assert (parseInt "123" == Some 123)
  _ <- assert (parseInt "abc" == None)
}
