@no_prelude
module integrationTests.stdlib.aivi.text.normalizeNFKC

use aivi
use aivi.testing

use aivi.text

subject = normalizeNFKC

@test
smoke = effect {
  _ <- testing.assert (length (normalizeNFKC "hello") == 5)
}
