@no_prelude
module integrationTests.stdlib.aivi.text.normalizeNFKC

use aivi
use aivi.testing (assert)

use aivi.text

subject = normalizeNFKC

@test
smoke = effect {
  _ <- assert (length (normalizeNFKC "hello") == 5)
}
