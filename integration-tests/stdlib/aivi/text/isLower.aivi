@no_prelude
module integrationTests.stdlib.aivi.text.isLower

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (isLower "a" == True)
  _ <- testing.assert (isLower "A" == False)
}
