@no_prelude
module integrationTests.stdlib.aivi.text.isLower

use aivi
use aivi.testing
use aivi.text

@test
smoke = effect {
  _ <- assert (isLower "a" == True)
  _ <- assert (isLower "A" == False)
}
