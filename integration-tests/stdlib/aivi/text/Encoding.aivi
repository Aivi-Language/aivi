@no_prelude
module integrationTests.stdlib.aivi.text.Encoding

use aivi
use aivi.testing
use aivi.text

touch : Encoding -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
