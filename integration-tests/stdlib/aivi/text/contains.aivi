@no_prelude
module integrationTests.stdlib.aivi.text.contains

use aivi
use aivi.testing (assert)

use aivi.text

subject = contains

@test
smoke = effect {
  _ <- assert (contains "ell" "hello" == True)
  _ <- assert (contains "z" "hello" == False)
}
