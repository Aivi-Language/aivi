@no_prelude
module integrationTests.stdlib.aivi.text.contains

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (contains "ell" "hello" == True)
  _ <- testing.assert (contains "z" "hello" == False)
}
