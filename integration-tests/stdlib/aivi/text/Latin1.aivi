@no_prelude
module integrationTests.stdlib.aivi.text.Latin1

use aivi
use aivi.testing
use aivi.text

touch : Latin1 -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
