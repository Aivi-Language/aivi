@no_prelude
module integrationTests.stdlib.aivi.text.replaceAll

use aivi
use aivi.testing (assert)

use aivi.text

subject = replaceAll

@test
smoke = effect {
  _ <- assert (replaceAll "l" "L" "hello" == "heLLo")
}
