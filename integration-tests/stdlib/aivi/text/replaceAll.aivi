@no_prelude
module integrationTests.stdlib.aivi.text.replaceAll

use aivi
use aivi.testing

use aivi.text

subject = replaceAll

@test
smoke = effect {
  _ <- testing.assert (replaceAll "l" "L" "hello" == "heLLo")
}
