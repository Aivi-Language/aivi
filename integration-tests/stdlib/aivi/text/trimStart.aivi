@no_prelude
module integrationTests.stdlib.aivi.text.trimStart

use aivi
use aivi.testing (assert)

use aivi.text

subject = trimStart

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
