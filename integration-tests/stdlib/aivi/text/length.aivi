@no_prelude
module integrationTests.stdlib.aivi.text.length

use aivi
use aivi.testing (assert)

use aivi.text

subject = length

@test
smoke = effect {
  _ <- assert (length "hello" == 5)
  _ <- assert (length "" == 0)
}
