@no_prelude
module integrationTests.stdlib.aivi.text.length

use aivi
use aivi.testing

use aivi.text

subject = length

@test
smoke = effect {
  _ <- testing.assert (length "hello" == 5)
  _ <- testing.assert (length "" == 0)
}
