@no_prelude
module integrationTests.stdlib.aivi.text.splitLines

use aivi
use aivi.testing (assert)

use aivi.text

subject = splitLines

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
