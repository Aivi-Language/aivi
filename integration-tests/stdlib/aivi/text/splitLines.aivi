@no_prelude
module integrationTests.stdlib.aivi.text.splitLines

use aivi
use aivi.testing

use aivi.text

subject = splitLines

@test
smoke = effect {
  _ <- testing.assert (splitLines "a\nb\nc" == ["a", "b", "c"])
  _ <- testing.assert (splitLines "a\r\nb" == ["a", "b"])
}
