@no_prelude
module integrationTests.stdlib.aivi.text.splitLines

use aivi
use aivi.testing (assert)

use aivi.text

subject = splitLines

@test
smoke = effect {
  _ <- assert (splitLines "a\nb\nc" == ["a", "b", "c"])
  _ <- assert (splitLines "a\r\nb" == ["a", "b"])
}
