@no_prelude
module integrationTests.stdlib.aivi.text.normalizeNFC

use aivi
use aivi.testing (assert)

use aivi.text

subject = normalizeNFC

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
