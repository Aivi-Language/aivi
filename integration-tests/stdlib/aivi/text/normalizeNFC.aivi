@no_prelude
module integrationTests.stdlib.aivi.text.normalizeNFC

use aivi
use aivi.testing

use aivi.text

subject = normalizeNFC

@test
smoke = effect {
  _ <- testing.assert (length (normalizeNFC "hello") == 5)
}
