@no_prelude
module integrationTests.stdlib.aivi.text.isDigit

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (isDigit "1" == True)
  _ <- testing.assert (isDigit "a" == False)
}
