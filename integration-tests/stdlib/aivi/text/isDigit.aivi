@no_prelude
module integrationTests.stdlib.aivi.text.isDigit

use aivi
use aivi.testing (assert)

use aivi.text

subject = isDigit

@test
smoke = effect {
  _ <- assert (isDigit "1" == True)
  _ <- assert (isDigit "a" == False)
}
