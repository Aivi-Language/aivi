@no_prelude
module integrationTests.stdlib.aivi.text.startsWith

use aivi
use aivi.testing

use aivi.text

subject = startsWith

@test
smoke = effect {
  _ <- testing.assert (startsWith "he" "hello" == True)
  _ <- testing.assert (startsWith "lo" "hello" == False)
}
