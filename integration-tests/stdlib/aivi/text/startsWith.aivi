@no_prelude
module integrationTests.stdlib.aivi.text.startsWith

use aivi
use aivi.testing (assert)

use aivi.text

subject = startsWith

@test
smoke = effect {
  _ <- assert (startsWith "he" "hello" == True)
  _ <- assert (startsWith "lo" "hello" == False)
}
