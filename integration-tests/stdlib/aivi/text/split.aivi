@no_prelude
module integrationTests.stdlib.aivi.text.split

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- assert (split "," "a,b,c" == ["a", "b", "c"])
  _ <- assert (split "." "a" == ["a"])
}
