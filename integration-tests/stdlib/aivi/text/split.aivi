@no_prelude
module integrationTests.stdlib.aivi.text.split

use aivi
use aivi.testing (assert)

use aivi.text

subject = split

@test
smoke = effect {
  _ <- assert (split "," "a,b,c" == ["a", "b", "c"])
  _ <- assert (split "." "a" == ["a"])
}
