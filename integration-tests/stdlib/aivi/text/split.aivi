@no_prelude
module integrationTests.stdlib.aivi.text.split

use aivi
use aivi.testing

use aivi.text

subject = split

@test
smoke = effect {
  _ <- testing.assert (split "," "a,b,c" == ["a", "b", "c"])
  _ <- testing.assert (split "." "a" == ["a"])
}
