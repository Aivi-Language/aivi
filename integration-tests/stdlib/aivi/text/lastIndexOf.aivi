@no_prelude
module integrationTests.stdlib.aivi.text.lastIndexOf

use aivi
use aivi.testing (assert)

use aivi.text

subject = lastIndexOf

@test
smoke = effect {
  _ <- assert (lastIndexOf "l" "hello" == Some 3)
  _ <- assert (lastIndexOf "z" "hello" == None)
}
