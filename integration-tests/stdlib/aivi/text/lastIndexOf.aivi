@no_prelude
module integrationTests.stdlib.aivi.text.lastIndexOf

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (lastIndexOf "l" "hello" == Some 3)
  _ <- testing.assert (lastIndexOf "z" "hello" == None)
}
