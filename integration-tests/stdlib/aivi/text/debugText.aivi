@no_prelude
module integrationTests.stdlib.aivi.text.debugText

use aivi
use aivi.testing (assert)

use aivi.text

subject = debugText

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
