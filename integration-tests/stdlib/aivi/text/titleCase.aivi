@no_prelude
module integrationTests.stdlib.aivi.text.titleCase

use aivi
use aivi.testing

use aivi.text

subject = titleCase

@test
smoke = effect {
  _ <- testing.assert (titleCase "hello world" == "Hello World")
}
