@no_prelude
module integrationTests.stdlib.aivi.text.titleCase

use aivi
use aivi.testing (assert)

use aivi.text

subject = titleCase

@test
smoke = effect {
  _ <- assert (titleCase "hello world" == "Hello World")
}
