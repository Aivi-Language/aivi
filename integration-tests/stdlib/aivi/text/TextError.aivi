@no_prelude
module integrationTests.stdlib.aivi.text.TextError

use aivi
use aivi.testing
use aivi.text


touch : TextError -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
