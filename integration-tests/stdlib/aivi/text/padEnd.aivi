@no_prelude
module integrationTests.stdlib.aivi.text.padEnd

use aivi
use aivi.testing

use aivi.text

subject = padEnd

@test
smoke = effect {
  _ <- testing.assert (padEnd 5 " " "abc" == "abc  ")
  _ <- testing.assert (padEnd 3 " " "abc" == "abc")
}
