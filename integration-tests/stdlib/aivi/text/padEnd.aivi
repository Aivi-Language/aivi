@no_prelude
module integrationTests.stdlib.aivi.text.padEnd

use aivi
use aivi.testing (assert)

use aivi.text

subject = padEnd

@test
smoke = effect {
  _ <- assert (padEnd 5 " " "abc" == "abc  ")
  _ <- assert (padEnd 3 " " "abc" == "abc")
}
