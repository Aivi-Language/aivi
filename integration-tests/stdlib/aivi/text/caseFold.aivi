@no_prelude
module integrationTests.stdlib.aivi.text.caseFold

use aivi
use aivi.testing

use aivi.text

subject = caseFold

@test
smoke = effect {
  _ <- testing.assert (caseFold "Hello" == "hello")
}
