@no_prelude
module integrationTests.stdlib.aivi.text.caseFold

use aivi
use aivi.testing (assert)

use aivi.text

subject = caseFold

@test
smoke = effect {
  _ <- assert (caseFold "Hello" == "hello")
}
