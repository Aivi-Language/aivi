@no_prelude
module integrationTests.stdlib.aivi.text.Bytes

use aivi
use aivi.testing
use aivi.text

touch : Bytes -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
