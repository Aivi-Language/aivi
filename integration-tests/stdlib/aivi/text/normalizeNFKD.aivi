@no_prelude
module integrationTests.stdlib.aivi.text.normalizeNFKD

use aivi
use aivi.testing (assert)

use aivi.text

subject = normalizeNFKD

@test
smoke = effect {
  _ <- assert (length (normalizeNFKD "hello") == 5)
}
