@no_prelude
module integrationTests.stdlib.aivi.text.indexOf

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- assert (indexOf "e" "hello" == Some 1)
  _ <- assert (indexOf "l" "hello" == Some 2)
  _ <- assert (indexOf "z" "hello" == None)
}
