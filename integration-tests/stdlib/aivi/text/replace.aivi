@no_prelude
module integrationTests.stdlib.aivi.text.replace

use aivi
use aivi.testing
use aivi.text

@test
smoke = effect {
  _ <- assert (replace "l" "L" "hello" == "heLlo") // Replaces first occurrence? Assuming standard behavior for replace vs replaceAll
}
