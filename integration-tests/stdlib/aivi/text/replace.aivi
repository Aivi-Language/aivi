@no_prelude
module integrationTests.stdlib.aivi.text.replace

use aivi
use aivi.testing

use aivi.text

subject = replace

@test
smoke = effect {
  _ <- testing.assert (replace "l" "L" "hello" == "heLlo") // Replaces first occurrence? Assuming standard behavior for replace vs replaceAll
}
