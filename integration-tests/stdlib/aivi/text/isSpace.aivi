@no_prelude
module integrationTests.stdlib.aivi.text.isSpace

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (isSpace " " == True)
  _ <- testing.assert (isSpace "a" == False)
}
