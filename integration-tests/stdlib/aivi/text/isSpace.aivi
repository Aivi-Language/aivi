@no_prelude
module integrationTests.stdlib.aivi.text.isSpace

use aivi
use aivi.testing (assert)

use aivi.text

subject = isSpace

@test
smoke = effect {
  _ <- assert (isSpace " " == True)
  _ <- assert (isSpace "a" == False)
}
