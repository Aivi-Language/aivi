@no_prelude
module integrationTests.stdlib.aivi.text.chunk

use aivi
use aivi.testing

use aivi.text

subject = chunk

@test
smoke = effect {
  _ <- testing.assert (chunk 2 "abcde" == ["ab", "cd", "e"])
}
