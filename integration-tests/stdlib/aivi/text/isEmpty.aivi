@no_prelude
module integrationTests.stdlib.aivi.text.isEmpty

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (isEmpty "" == True)
  _ <- testing.assert (isEmpty "a" == False)
}
