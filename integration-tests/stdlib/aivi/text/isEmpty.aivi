@no_prelude
module integrationTests.stdlib.aivi.text.isEmpty

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- assert (isEmpty "" == True)
  _ <- assert (isEmpty "a" == False)
}
