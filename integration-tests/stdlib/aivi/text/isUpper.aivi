@no_prelude
module integrationTests.stdlib.aivi.text.isUpper

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- assert (isUpper "A" == True)
  _ <- assert (isUpper "a" == False)
}
