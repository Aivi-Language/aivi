@no_prelude
module integrationTests.stdlib.aivi.text.isUpper

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (isUpper "A" == True)
  _ <- testing.assert (isUpper "a" == False)
}
