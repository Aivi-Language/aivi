@no_prelude
module integrationTests.stdlib.aivi.text.toBytes

use aivi
use aivi.testing (assert)

use aivi.text

subject = toBytes

@test
smoke = effect {
  // Assuming Bytes type or list of ints/bytes
  bytes = toBytes "abc"
  // Just checking not crashing for now, until Bytes type is explored
  _ <- assert True 
}
