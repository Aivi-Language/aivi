@no_prelude
module integrationTests.stdlib.aivi.text.endsWith

use aivi
use aivi.testing
use aivi.text

@test
smoke = effect {
  _ <- assert (endsWith "lo" "hello" == True)
  _ <- assert (endsWith "he" "hello" == False)
}
