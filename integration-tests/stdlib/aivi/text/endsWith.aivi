@no_prelude
module integrationTests.stdlib.aivi.text.endsWith

use aivi
use aivi.testing

use aivi.text

subject = endsWith

@test
smoke = effect {
  _ <- testing.assert (endsWith "lo" "hello" == True)
  _ <- testing.assert (endsWith "he" "hello" == False)
}
