@no_prelude
module integrationTests.stdlib.aivi.text.count

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (count "l" "hello" == 2)
  _ <- testing.assert (count "z" "hello" == 0)
}
