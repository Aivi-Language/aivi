@no_prelude
module integrationTests.stdlib.aivi.text.count

use aivi
use aivi.testing (assert)

use aivi.text

subject = count

@test
smoke = effect {
  _ <- assert (count "l" "hello" == 2)
  _ <- assert (count "z" "hello" == 0)
}
