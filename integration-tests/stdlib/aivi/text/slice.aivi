@no_prelude
module integrationTests.stdlib.aivi.text.slice

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- testing.assert (slice 0 2 "hello" == "he")
  _ <- testing.assert (slice 1 3 "hello" == "el")
}
