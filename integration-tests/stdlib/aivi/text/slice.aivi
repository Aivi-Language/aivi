@no_prelude
module integrationTests.stdlib.aivi.text.slice

use aivi
use aivi.testing (assert)

use aivi.text

subject = slice

@test
smoke = effect {
  _ <- assert (slice 0 2 "hello" == "he")
  _ <- assert (slice 1 3 "hello" == "el")
}
