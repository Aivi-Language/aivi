@no_prelude
module integrationTests.stdlib.aivi.text.padStart

use aivi
use aivi.testing (assert)

use aivi.text

subject = padStart

@test
smoke = effect {
  _ <- assert (padStart 5 " " "abc" == "  abc")
}
