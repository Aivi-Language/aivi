@no_prelude
module integrationTests.stdlib.aivi.text.isAlpha

use aivi
use aivi.testing

use aivi.text

subject = isAlpha

@test
smoke = effect {
  _ <- testing.assert (isAlpha "a" == True)
  _ <- testing.assert (isAlpha "1" == False)
}
