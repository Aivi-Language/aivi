@no_prelude
module integrationTests.stdlib.aivi.text.isAlpha

use aivi
use aivi.testing

use aivi.text



@test
smoke = effect {
  _ <- assert (isAlpha "a" == True)
  _ <- assert (isAlpha "1" == False)
}
