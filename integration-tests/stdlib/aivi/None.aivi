@no_prelude
module integrationTests.stdlib.aivi.None

use aivi
use aivi.testing
use aivi

@test
noneMatches = effect {
  opt = None
  _ <- opt ?
    | Some _ => assert False
    | None   => assert True
}
