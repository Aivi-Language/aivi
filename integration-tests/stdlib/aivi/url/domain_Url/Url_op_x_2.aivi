@no_prelude
module integrationTests.stdlib.aivi.url.domain_Url.Url_op_x_2

use aivi
use aivi.testing (assert)

use aivi.url

use aivi.url (domain Url)

left = { protocol: "x", host: "x", port: None, path: "x", query: [], hash: None }
right = "x"

subject = left - right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
