@no_prelude
module integrationTests.stdlib.aivi.Text

use aivi
use aivi.testing
use aivi

@test
smoke = effect {
  t = "Hello"
  assert (text.length t == 5)
  assert (text.toUpper t == "HELLO")
  assert (text.toLower t == "hello")
  assert (text.isEmpty "" == True)
  assert (text.isEmpty t == False)
}
