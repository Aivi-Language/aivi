@no_prelude
module integrationTests.stdlib.aivi.Text

use aivi
use aivi.testing

use aivi

Ref = Text

@test
smoke = effect {
  t = "Hello"
  _ <- testing.assert (Text.length t == 5)
  _ <- testing.assert (Text.toUpper t == "HELLO")
  _ <- testing.assert (Text.toLower t == "hello")
  _ <- testing.assert (Text.slice 0 2 t == "He")
  _ <- testing.assert (Text.isEmpty "" == True)
  _ <- testing.assert (Text.isEmpty t == False)
}
