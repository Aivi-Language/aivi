@no_prelude
module integrationTests.stdlib.aivi.ui.vElement

use aivi
use aivi.testing

use aivi.ui

subject = vElement

@test
smoke = effect {
  node = vElement "div" [] []
  _ <- testing.assert (renderHtml node == "<div data-aivi-node=\"root\"></div>")
}
