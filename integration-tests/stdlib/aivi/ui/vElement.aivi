@no_prelude
module integrationTests.stdlib.aivi.ui.vElement

use aivi
use aivi.testing (assert)

use aivi.ui

subject = vElement

@test
smoke = effect {
  node = vElement "div" [] []
  _ <- assert (renderHtml node == "<div data-aivi-node=\"root\"></div>")
}
