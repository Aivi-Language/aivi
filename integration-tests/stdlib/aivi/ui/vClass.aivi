@no_prelude
module integrationTests.stdlib.aivi.ui.vClass

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vClass "c"
  node = vElement "div" [attr] []
  _ <- assert (Text.length (renderHtml node) > 0)
}
