@no_prelude
module integrationTests.stdlib.aivi.ui.vText

use aivi
use aivi.testing

use aivi.ui

subject = vText

@test
smoke = effect {
  node = vText "hello"
  _ <- testing.assert (renderHtml node == "<span data-aivi-node=\"root\">hello</span>")
}
