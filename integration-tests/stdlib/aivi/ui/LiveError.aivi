@no_prelude
module integrationTests.stdlib.aivi.ui.LiveError

use aivi
use aivi.testing
use aivi.ui

touch : LiveError -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
