@no_prelude
module integrationTests.stdlib.aivi.ui.vOnFocus

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnFocus "evt"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
