@no_prelude
module integrationTests.stdlib.aivi.ui.Element

use aivi
use aivi.testing
use aivi.ui


touch : Element -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
