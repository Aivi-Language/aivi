@no_prelude
module integrationTests.stdlib.aivi.ui.ServerHtml.Effect

use aivi
use aivi.testing (assert)

use aivi.ui.ServerHtml

subject = Effect

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
