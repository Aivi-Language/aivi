@no_prelude
module integrationTests.stdlib.aivi.ui.ServerHtml.serveHttp

use aivi
use aivi.testing

use aivi.ui.ServerHtml

subject = serveHttp

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
