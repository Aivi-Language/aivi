@no_prelude
module integrationTests.stdlib.aivi.ui.ServerHtml.ClipboardError

use aivi
use aivi.testing (assert)

use aivi.ui.ServerHtml

Ref = ClipboardError

@test
smoke = effect {
  _ <- assert True
}
