@no_prelude
module integrationTests.stdlib.aivi.ui.ServerHtml.UrlInfo

use aivi
use aivi.testing
use aivi.ui.ServerHtml


touch : UrlInfo -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
