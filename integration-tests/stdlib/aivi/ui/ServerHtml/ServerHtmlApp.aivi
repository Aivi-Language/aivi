@no_prelude
module integrationTests.stdlib.aivi.ui.ServerHtml.ServerHtmlApp

use aivi
use aivi.testing (assert)

use aivi.ui.ServerHtml

subject = ServerHtmlApp

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
