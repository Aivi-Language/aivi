@no_prelude
module integrationTests.stdlib.aivi.ui.ServerHtml.PlatformEvent

use aivi
use aivi.testing
use aivi.ui.ServerHtml


touch : PlatformEvent -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
