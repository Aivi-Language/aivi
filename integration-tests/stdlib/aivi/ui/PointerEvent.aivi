@no_prelude
module integrationTests.stdlib.aivi.ui.PointerEvent

use aivi
use aivi.testing
use aivi.ui

touch : PointerEvent -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
