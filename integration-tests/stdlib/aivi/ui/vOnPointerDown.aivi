@no_prelude
module integrationTests.stdlib.aivi.ui.vOnPointerDown

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnPointerDown "evt"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
