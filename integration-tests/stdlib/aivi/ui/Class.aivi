@no_prelude
module integrationTests.stdlib.aivi.ui.Class

use aivi
use aivi.testing
use aivi.ui


touch : Class -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
