@no_prelude
module integrationTests.stdlib.aivi.ui.diff

use aivi
use aivi.testing

use aivi.ui



@test
smoke = effect {
  old = vText "a"
  new = vText "b"
  ops = diff old new
  // SetText "root" "b"
  _ <- testing.assert (length ops == 1)
}
