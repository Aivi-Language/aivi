@no_prelude
module integrationTests.stdlib.aivi.ui.diff

use aivi
use aivi.testing (assert)

use aivi.ui

subject = diff

@test
smoke = effect {
  old = vText "a"
  new = vText "b"
  ops = diff old new
  // SetText "root" "b"
  _ <- assert (length ops == 1)
}
