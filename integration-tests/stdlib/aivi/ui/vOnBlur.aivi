@no_prelude
module integrationTests.stdlib.aivi.ui.vOnBlur

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnBlur "evt"
  node = vElement "div" [attr] []
  _ <- assert (Text.length (renderHtml node) > 0)
}
