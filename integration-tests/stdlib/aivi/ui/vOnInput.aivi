@no_prelude
module integrationTests.stdlib.aivi.ui.vOnInput

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnInput "evt"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
