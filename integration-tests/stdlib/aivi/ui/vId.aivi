@no_prelude
module integrationTests.stdlib.aivi.ui.vId

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vId "x"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
