@no_prelude
module integrationTests.stdlib.aivi.ui.vOnKeyUp

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnKeyUp "evt"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
