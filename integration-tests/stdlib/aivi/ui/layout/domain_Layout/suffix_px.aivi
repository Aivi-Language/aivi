@no_prelude
module integrationTests.stdlib.aivi.ui.layout.domain_Layout.suffix_px

use aivi
use aivi.testing
use aivi.ui.layout
use aivi.ui.layout

@test
smoke = effect {
  x = 1px
  _ <- assert (x == x)
}
