@no_prelude
module integrationTests.stdlib.aivi.ui.layout.domain_Layout.tests

use aivi
use aivi.testing
use aivi.ui.layout
use aivi.ui.layout (domain Layout)

@test
literals_smoke = effect {
  _x <- 1px
  _y <- 1em
  _z <- 1rem
  _a <- 1vh
  _b <- 1vw
  assert True
}
