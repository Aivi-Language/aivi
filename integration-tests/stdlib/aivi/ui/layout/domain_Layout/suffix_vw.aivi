@no_prelude
module integrationTests.stdlib.aivi.ui.layout.domain_Layout.suffix_vw

use aivi
use aivi.testing
use aivi.ui.layout
use aivi.ui.layout

@test
smoke = effect {
  x = 1vw
  _ <- assert (x == x)
}
