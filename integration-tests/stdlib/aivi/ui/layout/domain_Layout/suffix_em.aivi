@no_prelude
module integrationTests.stdlib.aivi.ui.layout.domain_Layout.suffix_em

use aivi
use aivi.testing (assert)

use aivi.ui.layout

use aivi.ui.layout (domain Layout)

subject = 2em

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
