@no_prelude
module integrationTests.stdlib.aivi.ui.layout.UnitVal

use aivi
use aivi.testing
use aivi.ui.layout


touch : UnitVal -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
