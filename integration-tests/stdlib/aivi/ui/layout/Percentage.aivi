@no_prelude
module integrationTests.stdlib.aivi.ui.layout.Percentage

use aivi
use aivi.testing
use aivi.ui.layout


touch : Percentage -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
