@no_prelude
module integrationTests.stdlib.aivi.ui.Id

use aivi
use aivi.testing
use aivi.ui

touch : Id -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
