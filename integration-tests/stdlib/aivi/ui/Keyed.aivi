@no_prelude
module integrationTests.stdlib.aivi.ui.Keyed

use aivi
use aivi.testing
use aivi.ui

touch : Keyed -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
