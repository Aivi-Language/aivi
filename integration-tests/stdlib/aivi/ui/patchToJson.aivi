@no_prelude
module integrationTests.stdlib.aivi.ui.patchToJson

use aivi
use aivi.testing (assert)

use aivi.ui

subject = patchToJson

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
