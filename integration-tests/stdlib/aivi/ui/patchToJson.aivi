@no_prelude
module integrationTests.stdlib.aivi.ui.patchToJson

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  ops = diff (vText "a") (vText "b")
  json = patchToJson ops
  _ <- assert (Text.length json > 0)
}
