@no_prelude
module integrationTests.stdlib.aivi.ui.vOnClick

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnClick "evt"
  node = vElement "div" [attr] []
  _ <- assert (Text.length (renderHtml node) > 0)
}
