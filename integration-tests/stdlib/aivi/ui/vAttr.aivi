@no_prelude
module integrationTests.stdlib.aivi.ui.vAttr

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vAttr "id" "foo"
  node = vElement "div" [attr] []
  _ <- assert (renderHtml node == "<div data-aivi-node=\"root\" id=\"foo\"></div>")
}
