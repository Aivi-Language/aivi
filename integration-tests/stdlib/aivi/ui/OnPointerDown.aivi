@no_prelude
module integrationTests.stdlib.aivi.ui.OnPointerDown

use aivi
use aivi.testing (assert)

use aivi.ui

subject = OnPointerDown

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
