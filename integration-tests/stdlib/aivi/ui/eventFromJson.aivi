@no_prelude
module integrationTests.stdlib.aivi.ui.eventFromJson

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  // Unknown JSON details; just ensure decoder accepts something simple.
  ev = eventFromJson "{}"
  _ <- assert True
}
