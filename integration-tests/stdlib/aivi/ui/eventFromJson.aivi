@no_prelude
module integrationTests.stdlib.aivi.ui.eventFromJson

use aivi
use aivi.testing (assert)

use aivi.ui

subject = eventFromJson

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
