@no_prelude
module integrationTests.stdlib.aivi.ui.OnClick

use aivi
use aivi.testing
use aivi.ui

touch : OnClick -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
