@no_prelude
module integrationTests.stdlib.aivi.ui.vOnPointerUp

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnPointerUp "evt"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
