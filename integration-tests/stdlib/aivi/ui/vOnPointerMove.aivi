@no_prelude
module integrationTests.stdlib.aivi.ui.vOnPointerMove

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnPointerMove (_ => "evt")
  node = vElement "div" [attr] []
  _ <- assert (Text.length (renderHtml node) > 0)
}
