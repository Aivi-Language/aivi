@no_prelude
module integrationTests.stdlib.aivi.ui.vOnPointerMove

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnPointerMove "evt"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
