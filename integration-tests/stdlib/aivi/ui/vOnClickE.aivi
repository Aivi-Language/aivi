@no_prelude
module integrationTests.stdlib.aivi.ui.vOnClickE

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnClickE "evt"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
