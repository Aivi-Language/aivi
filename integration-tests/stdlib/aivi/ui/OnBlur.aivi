@no_prelude
module integrationTests.stdlib.aivi.ui.OnBlur

use aivi
use aivi.testing
use aivi.ui


touch : OnBlur -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
