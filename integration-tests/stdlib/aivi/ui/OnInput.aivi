@no_prelude
module integrationTests.stdlib.aivi.ui.OnInput

use aivi
use aivi.testing
use aivi.ui


touch : OnInput -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
