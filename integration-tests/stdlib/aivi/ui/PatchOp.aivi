@no_prelude
module integrationTests.stdlib.aivi.ui.PatchOp

use aivi
use aivi.testing
use aivi.ui

touch : PatchOp -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
