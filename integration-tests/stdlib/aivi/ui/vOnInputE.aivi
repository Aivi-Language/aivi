@no_prelude
module integrationTests.stdlib.aivi.ui.vOnInputE

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vOnInputE "evt"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
