@no_prelude
module integrationTests.stdlib.aivi.ui.vKeyed

use aivi
use aivi.testing
use aivi.ui

@test
smoke = effect {
  attr = vKeyed "k"
  node = vElement "div" [attr] []
  _ <- assert (length (renderHtml node) > 0)
}
