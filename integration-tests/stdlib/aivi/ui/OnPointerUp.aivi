@no_prelude
module integrationTests.stdlib.aivi.ui.OnPointerUp

use aivi
use aivi.testing (assert)

use aivi.ui

subject = OnPointerUp

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
