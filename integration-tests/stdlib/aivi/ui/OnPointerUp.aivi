@no_prelude
module integrationTests.stdlib.aivi.ui.OnPointerUp

use aivi
use aivi.testing
use aivi.ui

touch : OnPointerUp -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
