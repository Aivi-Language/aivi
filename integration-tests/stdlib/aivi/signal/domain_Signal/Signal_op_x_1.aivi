@no_prelude
module integrationTests.stdlib.aivi.signal.domain_Signal.Signal_op_x_1

use aivi
use aivi.testing (assert)

use aivi.signal

use aivi.signal (domain Signal)

left = { samples: [], rate: 1.0 }
right = { samples: [], rate: 1.0 }

subject = left + right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
