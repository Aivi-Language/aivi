@no_prelude
module integrationTests.stdlib.aivi.False

use aivi
use aivi.testing
use aivi

@test
falseBehavesLikeBool = effect {
  _ <- assert (!False)
  _ <- assert (False || True)
  _ <- assert (True != False)
}
