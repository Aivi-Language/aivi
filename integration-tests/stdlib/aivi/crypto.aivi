@no_prelude
@no_prelude
module integrationTests.stdlib.aivi.crypto

use aivi
use aivi.testing
use aivi.text
use aivi.crypto

@test
smoke = effect {
  hash = sha256 "aivi"
  assert (text.length hash == 64)
  assert (hash != "")
}
