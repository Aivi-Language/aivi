@no_prelude
module integrationTests.stdlib.aivi.i18n.message

use aivi
use aivi.testing (assert)

use aivi.i18n

subject = message

@test
smoke = effect {
  res = message "Hello, {name}!"
  m <- match res with
    Ok m => pure m
    Err e => fail e
  _ <- assert (m.body == "Hello, {name}!")
}
