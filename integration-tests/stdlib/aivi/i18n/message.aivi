@no_prelude
module integrationTests.stdlib.aivi.i18n.message

use aivi
use aivi.testing
use aivi.i18n

@test
smoke = effect {
  res = message "Hello!"
  m <- res ?
    | Ok m  => pure m
    | Err e => fail e
    _ <- assert (m.body == "Hello!")
}
