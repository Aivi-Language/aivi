@no_prelude
module integrationTests.stdlib.aivi.i18n.key

use aivi
use aivi.testing (assert)

use aivi.i18n

subject = key

@test
smoke = effect {
  res = key "greeting"
  k <- match res with
    Ok k => pure k
    Err e => fail e
  _ <- assert (k.body == "greeting")
}
