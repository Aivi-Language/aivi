@no_prelude
module integrationTests.stdlib.aivi.i18n.Locale

use aivi
use aivi.testing
use aivi.i18n

@test
smoke = effect {
  res = parseLocale "en_US"
  _ <- res ?
    | Ok l  => assert (l.tag == "en-US")
    | Err _ => assert False
}
