@no_prelude
module integrationTests.stdlib.aivi.i18n.bundleFromPropertiesFile

use aivi
use aivi.testing (assert)

use aivi.i18n

subject = bundleFromPropertiesFile

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
