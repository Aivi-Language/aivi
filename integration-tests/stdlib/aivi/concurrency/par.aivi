@no_prelude
module integrationTests.stdlib.aivi.concurrency.par

use aivi
use aivi.testing (assert)

use aivi.concurrency

subject = par

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
