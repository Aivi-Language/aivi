@no_prelude
module integrationTests.stdlib.aivi.concurrency.make

use aivi
use aivi.testing (assert)

use aivi.concurrency

subject = make

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
