@no_prelude
module integrationTests.stdlib.aivi.concurrency.Scope

use aivi
use aivi.testing
use aivi.concurrency

touch : Scope -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
