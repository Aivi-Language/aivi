@no_prelude
module integrationTests.stdlib.aivi.concurrency.ChannelError

use aivi
use aivi.testing
use aivi.concurrency


touch : ChannelError -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
