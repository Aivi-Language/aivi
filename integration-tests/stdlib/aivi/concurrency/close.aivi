@no_prelude
module integrationTests.stdlib.aivi.concurrency.close

use aivi
use aivi.testing (assert)

use aivi.concurrency

subject = close

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
