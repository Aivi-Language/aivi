@no_prelude
module integrationTests.stdlib.aivi.linalg.Mat

use aivi
use aivi.testing (assert)

use aivi.linalg

Ref = Mat

@test
smoke = effect {
  m = { rows: 2, cols: 2, data: [1.0, 0.0, 0.0, 1.0] }
  _ <- assert (m.rows == 2)
}
