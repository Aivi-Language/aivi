@no_prelude
module integrationTests.stdlib.aivi.linalg.dot

use aivi
use aivi.testing

use aivi.linalg



@test
smoke = effect {
  v1 = { size: 2, data: [1.0, 2.0] }
  v2 = { size: 2, data: [3.0, 4.0] }
  d = dot v1 v2
  _ <- testing.assert (d == 11.0)
}
