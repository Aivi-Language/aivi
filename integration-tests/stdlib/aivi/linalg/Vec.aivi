@no_prelude
module integrationTests.stdlib.aivi.linalg.Vec

use aivi
use aivi.testing

use aivi.linalg

Ref = Vec

@test
smoke = effect {
  v = { size: 3, data: [1.0, 2.0, 3.0] }
  _ <- testing.assert (v.size == 3)
}
