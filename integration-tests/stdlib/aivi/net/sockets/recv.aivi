@no_prelude
module integrationTests.stdlib.aivi.net.sockets.recv

use aivi
use aivi.testing (assert)

use aivi.net.sockets

subject = recv

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
