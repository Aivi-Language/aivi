@no_prelude
module integrationTests.stdlib.aivi.net.sockets.SocketError

use aivi
use aivi.testing
use aivi.net.sockets

touch : SocketError -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
