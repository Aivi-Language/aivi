@no_prelude
module integrationTests.stdlib.aivi.net.sockets.connect

use aivi
use aivi.testing

use aivi.net.sockets

subject = connect

@test
smoke = effect {
  // connect returns a source yielding Result Connection SocketError
  src = connect "localhost" 8080
  res <- src
  _ <- assert True
}
