@no_prelude
module integrationTests.stdlib.aivi.net.sockets.Connection

use aivi
use aivi.testing
use aivi.net.sockets


touch : Connection -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
