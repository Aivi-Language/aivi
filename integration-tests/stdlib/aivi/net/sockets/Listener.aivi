@no_prelude
module integrationTests.stdlib.aivi.net.sockets.Listener

use aivi
use aivi.testing (assert)

use aivi.net.sockets

subject = Listener

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
