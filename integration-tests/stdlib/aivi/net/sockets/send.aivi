@no_prelude
module integrationTests.stdlib.aivi.net.sockets.send

use aivi
use aivi.testing (assert)

use aivi.net.sockets

subject = send

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
