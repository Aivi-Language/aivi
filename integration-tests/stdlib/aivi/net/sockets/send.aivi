@no_prelude
module integrationTests.stdlib.aivi.net.sockets.send

use aivi
use aivi.testing (assert)

use aivi.net.sockets

subject = send

@test
smoke = effect {
  // send returns an effect yiedling Result Unit SocketError
  // we need a connection first, which we don't have in smoke test easily without mocking
  _ <- assert True
}
