@no_prelude
module integrationTests.stdlib.aivi.net.sockets.accept

use aivi
use aivi.testing (assert)

use aivi.net.sockets

subject = accept

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
