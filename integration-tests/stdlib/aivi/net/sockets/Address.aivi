@no_prelude
module integrationTests.stdlib.aivi.net.sockets.Address

use aivi
use aivi.testing
use aivi.net.sockets


touch : Address -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
