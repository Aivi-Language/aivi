@no_prelude
module integrationTests.stdlib.aivi.net.httpServer

use aivi
use aivi.testing (assert)

use aivi.net

subject = httpServer

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
