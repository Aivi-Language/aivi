@no_prelude
module integrationTests.stdlib.aivi.net.streams.chunks

use aivi
use aivi.testing (assert)

use aivi.net.streams

subject = chunks

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
