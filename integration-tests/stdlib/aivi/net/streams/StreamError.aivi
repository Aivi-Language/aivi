@no_prelude
module integrationTests.stdlib.aivi.net.streams.StreamError

use aivi
use aivi.testing
use aivi.net.streams

touch : StreamError -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
