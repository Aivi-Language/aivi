@no_prelude
module integrationTests.stdlib.aivi.net.streams.toSocket

use aivi
use aivi.testing (assert)

use aivi.net.streams

subject = toSocket

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
