@no_prelude
module integrationTests.stdlib.aivi.net.streams.Stream

use aivi
use aivi.testing (assert)

use aivi.net.streams

subject = Stream

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
