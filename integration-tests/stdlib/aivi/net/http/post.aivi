@no_prelude
module integrationTests.stdlib.aivi.net.http.post

use aivi
use aivi.testing

use aivi.net.http

subject = post

@test
smoke = effect {
  src = post "http://example.com" "body"
  res <- src
  _ <- assert True
}
