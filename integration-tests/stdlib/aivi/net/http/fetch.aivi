@no_prelude
module integrationTests.stdlib.aivi.net.http.fetch

use aivi
use aivi.testing (assert)

use aivi.net.http

subject = fetch

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
