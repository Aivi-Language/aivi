@no_prelude
module integrationTests.stdlib.aivi.net.http.fetch

use aivi
use aivi.testing

use aivi.net.http



@test
smoke = effect {
  req = { method: "GET", url: "http://example.com", headers: [], body: None }
  src = fetch req
  res <- src
  _ <- assert True
}
