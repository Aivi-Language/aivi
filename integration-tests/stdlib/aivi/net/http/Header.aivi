@no_prelude
module integrationTests.stdlib.aivi.net.http.Header

use aivi
use aivi.testing
use aivi.net.http

touch : Header -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
