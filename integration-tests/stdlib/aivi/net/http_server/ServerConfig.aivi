@no_prelude
module integrationTests.stdlib.aivi.net.http_server.ServerConfig

use aivi
use aivi.testing (assert)

use aivi.net.http_server

Ref = ServerConfig

@test
smoke = effect {
  _ <- assert True
}
