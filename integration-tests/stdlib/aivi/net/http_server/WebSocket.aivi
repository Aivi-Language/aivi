@no_prelude
module integrationTests.stdlib.aivi.net.http_server.WebSocket

use aivi
use aivi.testing (assert)

use aivi.net.http_server

subject = WebSocket

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
