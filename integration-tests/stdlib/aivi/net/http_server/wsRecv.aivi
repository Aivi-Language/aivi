@no_prelude
module integrationTests.stdlib.aivi.net.http_server.wsRecv

use aivi
use aivi.testing

use aivi.net.http_server

subject = wsRecv

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
