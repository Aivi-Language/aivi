@no_prelude
module integrationTests.stdlib.aivi.net.http_server.stop

use aivi
use aivi.testing

use aivi.net.http_server

subject = stop

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
