@no_prelude
module integrationTests.stdlib.aivi.net.http_server.wsClose

use aivi
use aivi.testing

use aivi.net.http_server

subject = wsClose

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
