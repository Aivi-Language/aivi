@no_prelude
module integrationTests.stdlib.aivi.net.http_server.Server

use aivi
use aivi.testing (assert)

use aivi.net.http_server

subject = Server

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
