@no_prelude
module integrationTests.stdlib.aivi.net.http_server.HttpError

use aivi
use aivi.testing
use aivi.net.http_server


touch : HttpError -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
