@no_prelude
module integrationTests.stdlib.aivi.net.http_server.listen

use aivi
use aivi.testing (assert)

use aivi.net.http_server

subject = listen

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
