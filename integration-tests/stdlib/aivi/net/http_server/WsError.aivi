@no_prelude
module integrationTests.stdlib.aivi.net.http_server.WsError

use aivi
use aivi.testing
use aivi.net.http_server

touch : WsError -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
