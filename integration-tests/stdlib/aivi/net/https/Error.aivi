@no_prelude
module integrationTests.stdlib.aivi.net.https.Error

use aivi
use aivi.testing
use aivi.net.https

touch : Error -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
