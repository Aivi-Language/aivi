@no_prelude
module integrationTests.stdlib.aivi.net.https.post

use aivi
use aivi.testing (assert)

use aivi.net.https

subject = post

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
