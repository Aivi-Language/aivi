@no_prelude
module integrationTests.stdlib.aivi.net.https.Response

use aivi
use aivi.testing
use aivi.net.https


touch : Response -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
