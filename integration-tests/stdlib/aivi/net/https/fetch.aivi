@no_prelude
module integrationTests.stdlib.aivi.net.https.fetch

use aivi
use aivi.testing (assert)

use aivi.net.https

subject = fetch

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
