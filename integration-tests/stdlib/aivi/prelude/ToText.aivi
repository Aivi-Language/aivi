@no_prelude
module integrationTests.stdlib.aivi.prelude.ToText

use aivi
use aivi.testing (assert)

use aivi.prelude

subject = ToText

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
