@no_prelude
module integrationTests.stdlib.aivi.prelude.ToText

use aivi
use aivi.testing
use aivi.prelude

@test
toTextWorks = effect {
  _ <- assertEq (toText "abc") "abc"
  _ <- assertEq (toText 123) "123"
}
