@no_prelude
module integrationTests.stdlib.aivi.prelude.Char

use aivi
use aivi.testing
use aivi.prelude
use aivi.text

@test
charPredicates = effect {
  _ <- pure isAlpha
  _ <- assertEq (1 + 1) 2
}
