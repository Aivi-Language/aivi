@no_prelude
module integrationTests.stdlib.aivi.prelude.Char

use aivi
use aivi.testing
use aivi.prelude
use aivi.text

@test
charPredicates = effect {
  _ <- assert (isAlpha "a" == True)
  _ <- assert (isAlpha "0" == False)
}
