@no_prelude
module integrationTests.stdlib.aivi.prelude.Tuple

use aivi
use aivi.testing
use aivi.prelude

@test
tupleMatches = effect {
  t = (1, "two")
  res = t ?
    | (a, b) => if a == 1 && b == "two" then True else False
    | _      => False
    _ <- assert (res == True)
}
