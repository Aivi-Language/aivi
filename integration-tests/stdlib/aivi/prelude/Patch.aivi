@no_prelude
module integrationTests.stdlib.aivi.prelude.Patch

use aivi
use aivi.testing
use aivi.prelude

@test
recordPatchingWorks = effect {
  user = { name: "A", age: 30 }
  older = user <| { age: _ + 1 }
  _ <- assertEq older.age 31
}
