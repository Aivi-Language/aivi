@no_prelude
module integrationTests.stdlib.aivi.Some

use aivi
use aivi.testing
use aivi

@test
someMatches = effect {
  opt = Some 7
  _ <- opt ?
    | Some n => assertEq n 7
    | None   => assert False
}
