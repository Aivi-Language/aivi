@no_prelude
module integrationTests.stdlib.aivi.i18n

use aivi
use aivi.testing
use aivi.i18n

@test
smoke = effect {
  loc = parseLocale "en-US"
  _ <- loc ?
    | Ok l  => assert (l.tag == "en-US")
    | Err _ => assert False
}
