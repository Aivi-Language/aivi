@no_prelude
module integrationTests.stdlib.aivi.concurrent

use aivi
use aivi.testing
use aivi

@test
parReturnsPair = effect {
  (a, b) <- concurrent.par (pure 1) (pure 2)
  assertEq a 1
  assertEq b 2
}
