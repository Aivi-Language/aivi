@no_prelude
module integrationTests.stdlib.aivi.Err

use aivi
use aivi.testing
use aivi

@test
errMatches = effect {
  res = Err "boom"
  res ?
    | Ok _  => assert False
    | Err e => assertEq e "boom"
}
