@no_prelude
module integrationTests.stdlib.aivi.linear_algebra.Vec

use aivi
use aivi.testing
use aivi.linear_algebra

@test
smoke = effect {
  v = { size: 2, data: [1.0, 2.0] }
  _ <- assert (v.size == 2)
}
