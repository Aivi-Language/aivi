@no_prelude
module integrationTests.stdlib.aivi.linear_algebra.Mat

use aivi
use aivi.testing
use aivi.linear_algebra

@test
smoke = effect {
  m = { rows: 2, cols: 2, data: [1.0, 0.0, 0.0, 1.0] }
  _ <- assert (m.rows == 2)
}
