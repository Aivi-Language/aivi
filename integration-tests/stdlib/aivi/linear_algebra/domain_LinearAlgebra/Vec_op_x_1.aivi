@no_prelude
module integrationTests.stdlib.aivi.linear_algebra.domain_LinearAlgebra.Vec_op_x_1

use aivi
use aivi.testing (assert)

use aivi.linear_algebra

use aivi.linear_algebra (domain LinearAlgebra)

left = { size: 1, data: [] }
right = { size: 1, data: [] }

subject = left + right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
