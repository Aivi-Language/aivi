@no_prelude
module integrationTests.stdlib.aivi.linear_algebra.dot

use aivi
use aivi.testing

use aivi.linear_algebra



@test
smoke = effect {
  v1 = { size: 2, data: [1.0, 2.0] }
  v2 = { size: 2, data: [3.0, 4.0] }
  _ <- assert (dot v1 v2 == 11.0)
}
