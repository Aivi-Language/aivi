@no_prelude
module integrationTests.stdlib.aivi.Bytes

use aivi
use aivi.testing
use aivi.text

@test
smoke = effect {
  b = toBytes Utf8 "abc"
  res = fromBytes Utf8 b
  _ <- res ?
    | Ok s  => assertEq s "abc"
    | Err _ => assert False
}
