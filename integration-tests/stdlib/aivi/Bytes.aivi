@no_prelude
module integrationTests.stdlib.aivi.Bytes

use aivi
use aivi.testing

use aivi

Ref = Bytes

@test
smoke = effect {
  b = toBytes "abc"
  s = fromBytes b
  _ <- testing.assert (s == "abc")
}
