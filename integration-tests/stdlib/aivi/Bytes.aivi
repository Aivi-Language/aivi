@no_prelude
module integrationTests.stdlib.aivi.Bytes

use aivi
use aivi.testing (assert)

use aivi

Ref = Bytes

@test
smoke = effect {
  b = toBytes "abc"
  s = fromBytes b
  _ <- assert (s == "abc")
}
