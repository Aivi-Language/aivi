@no_prelude
module integrationTests.stdlib.aivi.Char

use aivi
use aivi.testing (assert)

use aivi

Ref = Char

@test
smoke = effect {
  c = 'a'
  _ <- assert (isAlpha c == True)
  _ <- assert (isDigit c == False)
}
