@no_prelude
module integrationTests.stdlib.aivi.calendar.daysInMonth

use aivi
use aivi.testing (assert)

use aivi.calendar

subject = daysInMonth

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
