@no_prelude
module integrationTests.stdlib.aivi.calendar.addMonths

use aivi
use aivi.testing (assert)

use aivi.calendar

subject = addMonths

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
