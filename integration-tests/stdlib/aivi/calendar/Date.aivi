@no_prelude
module integrationTests.stdlib.aivi.calendar.Date

use aivi
use aivi.testing

use aivi.calendar

Ref = Date

@test
smoke = effect {
  d = { year: 2024, month: 2, day: 29 }
  _ <- testing.assert (d.year == 2024)
}
