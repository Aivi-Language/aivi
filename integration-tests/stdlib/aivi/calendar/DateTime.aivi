@no_prelude
module integrationTests.stdlib.aivi.calendar.DateTime

use aivi
use aivi.testing
use aivi.calendar


touch : DateTime -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
