@no_prelude
module integrationTests.stdlib.aivi.calendar.addYears

use aivi
use aivi.testing (assert)

use aivi.calendar

subject = addYears

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
