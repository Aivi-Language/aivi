@no_prelude
module integrationTests.stdlib.aivi.calendar.isLeapYear

use aivi
use aivi.testing (assert)

use aivi.calendar

subject = isLeapYear

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
