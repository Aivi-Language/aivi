@no_prelude
module integrationTests.stdlib.aivi.calendar.domain_Calendar.Date_op_x_1

use aivi
use aivi.testing (assert)

use aivi.calendar

use aivi.calendar (domain Calendar)

left = { year: 1, month: 1, day: 1 }
right = 2d

subject = left + right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
