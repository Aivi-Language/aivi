@no_prelude
module integrationTests.stdlib.aivi.calendar.domain_Calendar.suffix_d

use aivi
use aivi.testing

use aivi.calendar

use aivi.calendar



@test
smoke = effect {
  d = 5d
  _ <- testing.assert (d == 5) // Duration in days?
}
