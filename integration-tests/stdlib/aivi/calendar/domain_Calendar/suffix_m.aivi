@no_prelude
module integrationTests.stdlib.aivi.calendar.domain_Calendar.suffix_m

use aivi
use aivi.testing (assert)

use aivi.calendar

use aivi.calendar (domain Calendar)

subject = 2m

@test
smoke = effect {
  m = 1m
  _ <- assert (m == 1)
}
