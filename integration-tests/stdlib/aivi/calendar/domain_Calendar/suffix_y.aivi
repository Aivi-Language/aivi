@no_prelude
module integrationTests.stdlib.aivi.calendar.domain_Calendar.suffix_y

use aivi
use aivi.testing
use aivi.calendar
use aivi.calendar

@test
smoke = effect {
  y = 1y
  _ <- assert (y == 1)
}
