@no_prelude
module integrationTests.stdlib.aivi.calendar.domain_Calendar.suffix_y

use aivi
use aivi.testing (assert)

use aivi.calendar

use aivi.calendar (domain Calendar)

subject = 2y

@test
smoke = effect {
  y = 1y
  _ <- assert (y == 1)
}
