@no_prelude
module integrationTests.stdlib.aivi.calendar.domain_Calendar.suffix_y

use aivi
use aivi.testing

use aivi.calendar

use aivi.calendar

subject = 2y

@test
smoke = effect {
  y = 1y
  _ <- testing.assert (y == 1)
}
