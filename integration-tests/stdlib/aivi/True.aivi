@no_prelude
module integrationTests.stdlib.aivi.True

use aivi
use aivi.testing
use aivi

@test
trueBehavesLikeBool = effect {
  _ <- assert True
  _ <- assert (True && True)
  _ <- assert (True || False)
}
