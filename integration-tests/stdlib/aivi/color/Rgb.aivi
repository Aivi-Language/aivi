@no_prelude
module integrationTests.stdlib.aivi.color.Rgb

use aivi
use aivi.testing
use aivi.color

@test
smoke = effect {
  rgb = { r: 10, g: 20, b: 30 }
  _ <- assert (rgb.g > rgb.r)
  _ <- assert (rgb.b > rgb.g)
}
