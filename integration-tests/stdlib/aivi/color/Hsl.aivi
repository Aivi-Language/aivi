@no_prelude
module integrationTests.stdlib.aivi.color.Hsl

use aivi
use aivi.testing
use aivi.color

@test
smoke = effect {
  h = { h: 240.0, s: 0.5, l: 0.5 }
  _ <- assert (h.s == 0.5)
  rgb = toRgb h
  _ <- assert (rgb.b >= rgb.r)
}
