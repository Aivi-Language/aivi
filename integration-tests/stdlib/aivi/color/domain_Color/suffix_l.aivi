@no_prelude
module integrationTests.stdlib.aivi.color.domain_Color.suffix_l

use aivi
use aivi.testing
use aivi.color
use aivi.color (domain Color)

@test
smoke = effect {
  rgb = { r: 20, g: 20, b: 20 }
  adjusted = rgb + 1l
  _ <- assert (toHex adjusted != toHex rgb)
}
