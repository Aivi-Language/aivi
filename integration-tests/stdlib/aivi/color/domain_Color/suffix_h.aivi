@no_prelude
module integrationTests.stdlib.aivi.color.domain_Color.suffix_h

use aivi
use aivi.testing
use aivi.color

@test
smoke = effect {
  rgb = { r: 10, g: 20, b: 30 }
  adjusted = rgb + suffix_h
  _ <- assert (toHex adjusted != toHex rgb)
}
