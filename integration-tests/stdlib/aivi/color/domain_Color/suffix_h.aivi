@no_prelude
module integrationTests.stdlib.aivi.color.domain_Color.suffix_h

use aivi
use aivi.testing

use aivi.color

use aivi.color

subject = 2h

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
