@no_prelude
module integrationTests.stdlib.aivi.color.domain_Color.suffix_s

use aivi
use aivi.testing
use aivi.color

@test
smoke = effect {
  rgb = { r: 0, g: 50, b: 100 }
  adjusted = rgb + suffix_s
  _ <- assert (toHex adjusted != toHex rgb)
}
