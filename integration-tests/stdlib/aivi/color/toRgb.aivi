@no_prelude
module integrationTests.stdlib.aivi.color.toRgb

use aivi
use aivi.testing
use aivi.color

@test
smoke = effect {
  hsl = { h: 120.0, s: 0.5, l: 0.5 }
  rgb = toRgb hsl
  _ <- assert (rgb.g >= rgb.r)
  _ <- assert (rgb.b >= rgb.r)
}
