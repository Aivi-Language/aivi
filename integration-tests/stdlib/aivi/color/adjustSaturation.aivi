@no_prelude
module integrationTests.stdlib.aivi.color.adjustSaturation

use aivi
use aivi.testing (assert)

use aivi.color

subject = adjustSaturation

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
