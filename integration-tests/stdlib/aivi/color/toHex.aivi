@no_prelude
module integrationTests.stdlib.aivi.color.toHex

use aivi
use aivi.testing
use aivi.color
use aivi.text

@test
smoke = effect {
  rgb = { r: 255, g: 255, b: 255 }
  hex = toHex rgb
  _ <- assert (text.length hex == 7)
  _ <- assert (text.startsWith hex "#")
}
