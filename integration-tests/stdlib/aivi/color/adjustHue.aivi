@no_prelude
module integrationTests.stdlib.aivi.color.adjustHue

use aivi
use aivi.testing
use aivi.color

@test
smoke = effect {
  rgb = { r: 255, g: 0, b: 0 }
  shifted = adjustHue rgb 120
  _ <- assert (shifted.g > 0 || shifted.b > 0)
}
