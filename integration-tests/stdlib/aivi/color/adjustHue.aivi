@no_prelude
module integrationTests.stdlib.aivi.color.adjustHue

use aivi
use aivi.testing (assert)

use aivi.color

subject = adjustHue

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
