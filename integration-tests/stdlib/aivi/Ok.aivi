@no_prelude
module integrationTests.stdlib.aivi.Ok

use aivi
use aivi.testing
use aivi

@test
okMatches = effect {
  res = Ok 123
  _ <- res ?
    | Ok n  => assertEq n 123
    | Err _ => assert False
}
