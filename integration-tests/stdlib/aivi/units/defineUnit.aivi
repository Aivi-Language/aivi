@no_prelude
module integrationTests.stdlib.aivi.units.defineUnit

use aivi
use aivi.testing (assert)

use aivi.units

subject = defineUnit

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
