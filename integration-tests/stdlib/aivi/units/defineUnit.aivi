@no_prelude
module integrationTests.stdlib.aivi.units.defineUnit

use aivi
use aivi.testing
use aivi.units

@test
smoke = effect {
  m = defineUnit "m" 1.0
  km = defineUnit "km" 1000.0
  _ <- assert (m.name == "m")
  _ <- assert (km.factor == 1000.0)
}
