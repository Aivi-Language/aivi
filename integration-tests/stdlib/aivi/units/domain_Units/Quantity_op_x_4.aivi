@no_prelude
module integrationTests.stdlib.aivi.units.domain_Units.Quantity_op_x_4

use aivi
use aivi.testing (assert)

use aivi.units

use aivi.units (domain Units)

left = { value: 1.0, unit: { name: "x", factor: 1.0 } }
right = 1.0

subject = left / right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
