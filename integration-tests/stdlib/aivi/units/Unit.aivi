@no_prelude
module integrationTests.stdlib.aivi.units.Unit

use aivi
use aivi.testing
use aivi.units

@test
smoke = effect {
  u = defineUnit "s" 1.0
  _ <- assert (u.name == "s")
  _ <- assert (u.factor == 1.0)
}
