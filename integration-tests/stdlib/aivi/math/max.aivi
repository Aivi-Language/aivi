@no_prelude
module integrationTests.stdlib.aivi.math.max

use aivi
use aivi.testing

use aivi.math

subject = max

@test
smoke = effect {
  _ <- testing.assert (max 1.0 2.0 == 2.0)
  _ <- testing.assert (max 2.0 1.0 == 2.0)
}
