@no_prelude
module integrationTests.stdlib.aivi.math.isNaN

use aivi
use aivi.testing (assert)

use aivi.math

subject = isNaN

@test
smoke = effect {
  _ <- assert (isNaN nan == True)
  _ <- assert (isNaN 1.0 == False)
}
