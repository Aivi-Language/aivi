@no_prelude
module integrationTests.stdlib.aivi.math.isNaN

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (isNaN nan == True)
  _ <- testing.assert (isNaN 1.0 == False)
}
