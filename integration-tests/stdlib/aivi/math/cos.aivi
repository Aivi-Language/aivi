@no_prelude
module integrationTests.stdlib.aivi.math.cos

use aivi
use aivi.testing (assert)

use aivi.math

subject = cos

@test
smoke = effect {
  _ <- assert (abs (cos 0.0-1.0) < 0.0001)
  _ <- assert (abs (cos (pi / 2.0)) < 0.0001)
}
