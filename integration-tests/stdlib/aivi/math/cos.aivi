@no_prelude
module integrationTests.stdlib.aivi.math.cos

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (abs (cos 0.0-1.0) < 0.0001)
  _ <- testing.assert (abs (cos (pi / 2.0)) < 0.0001)
}
