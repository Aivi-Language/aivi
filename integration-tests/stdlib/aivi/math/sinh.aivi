@no_prelude
module integrationTests.stdlib.aivi.math.sinh

use aivi
use aivi.testing (assert)

use aivi.math

subject = sinh

@test
smoke = effect {
  _ <- assert (abs (sinh 0.0) < 0.0001)
  _ <- assert (abs (sinh 1.0-1.1752) < 0.0001)
}
