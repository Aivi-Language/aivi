@no_prelude
module integrationTests.stdlib.aivi.math.floor

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (floor 1.2 == 1.0)
  _ <- testing.assert (floor 1.9 == 1.0)
  _ <- testing.assert (floor (-1.2) == -2.0)
}
