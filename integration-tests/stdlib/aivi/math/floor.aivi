@no_prelude
module integrationTests.stdlib.aivi.math.floor

use aivi
use aivi.testing (assert)

use aivi.math

subject = floor

@test
smoke = effect {
  _ <- assert (floor 1.2 == 1.0)
  _ <- assert (floor 1.9 == 1.0)
  _ <- assert (floor (-1.2) == -2.0)
}
