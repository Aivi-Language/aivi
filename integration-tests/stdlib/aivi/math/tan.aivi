@no_prelude
module integrationTests.stdlib.aivi.math.tan

use aivi
use aivi.testing

use aivi.math

subject = tan

@test
smoke = effect {
  _ <- testing.assert (abs (tan 0.0) < 0.0001)
  _ <- testing.assert (abs (tan (pi / 4.0) -1.0) < 0.0001)
}
