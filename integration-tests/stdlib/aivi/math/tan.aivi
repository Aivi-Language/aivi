@no_prelude
module integrationTests.stdlib.aivi.math.tan

use aivi
use aivi.testing (assert)

use aivi.math

subject = tan

@test
smoke = effect {
  _ <- assert (abs (tan 0.0) < 0.0001)
  _ <- assert (abs (tan (pi / 4.0) -1.0) < 0.0001)
}
