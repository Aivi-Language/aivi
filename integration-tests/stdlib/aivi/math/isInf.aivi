@no_prelude
module integrationTests.stdlib.aivi.math.isInf

use aivi
use aivi.testing
use aivi.math

@test
smoke = effect {
  _ <- assert (isInf inf == True)
  _ <- assert (isInf (- inf) == True)
  _ <- assert (isInf 1.0 == False)
}
