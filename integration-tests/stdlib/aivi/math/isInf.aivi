@no_prelude
module integrationTests.stdlib.aivi.math.isInf

use aivi
use aivi.testing

use aivi.math

subject = isInf

@test
smoke = effect {
  _ <- testing.assert (isInf inf == True)
  _ <- testing.assert (isInf (- inf) == True)
  _ <- testing.assert (isInf 1.0 == False)
}
