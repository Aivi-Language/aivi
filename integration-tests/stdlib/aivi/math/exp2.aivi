@no_prelude
module integrationTests.stdlib.aivi.math.exp2

use aivi
use aivi.testing

use aivi.math

subject = exp2

@test
smoke = effect {
  _ <- testing.assert (abs (exp2 3.0-8.0) < 0.0001)
  _ <- testing.assert (abs (exp2 0.0-1.0) < 0.0001)
}
