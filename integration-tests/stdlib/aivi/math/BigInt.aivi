@no_prelude
module integrationTests.stdlib.aivi.math.BigInt

use aivi
use aivi.testing
use aivi.math


touch : BigInt -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
