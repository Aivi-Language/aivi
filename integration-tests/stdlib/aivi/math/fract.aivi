@no_prelude
module integrationTests.stdlib.aivi.math.fract

use aivi
use aivi.testing (assert)

use aivi.math

subject = fract

@test
smoke = effect {
  _ <- assert (abs (fract 1.5-0.5) < 0.0001)
  _ <- assert (abs (fract (-1.5) - (-0.5)) < 0.0001)
}
