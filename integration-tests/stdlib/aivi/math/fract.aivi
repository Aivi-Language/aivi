@no_prelude
module integrationTests.stdlib.aivi.math.fract

use aivi
use aivi.testing

use aivi.math

subject = fract

@test
smoke = effect {
  _ <- testing.assert (abs (fract 1.5-0.5) < 0.0001)
  _ <- testing.assert (abs (fract (-1.5) - (-0.5)) < 0.0001)
}
