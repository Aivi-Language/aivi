@no_prelude
module integrationTests.stdlib.aivi.math.lcm

use aivi
use aivi.testing (assert)

use aivi.math

subject = lcm

@test
smoke = effect {
  _ <- assert (lcm 4 6 == 12)
  _ <- assert (lcm 3 5 == 15)
}
