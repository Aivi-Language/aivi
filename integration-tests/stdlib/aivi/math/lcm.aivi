@no_prelude
module integrationTests.stdlib.aivi.math.lcm

use aivi
use aivi.testing

use aivi.math

subject = lcm

@test
smoke = effect {
  _ <- testing.assert (lcm 4 6 == 12)
  _ <- testing.assert (lcm 3 5 == 15)
}
