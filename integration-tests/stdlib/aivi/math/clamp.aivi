@no_prelude
module integrationTests.stdlib.aivi.math.clamp

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (clamp 10.0 5.0 0.0 == 5.0)
  _ <- testing.assert (clamp (-5.0) 5.0 0.0 == 0.0)
  _ <- testing.assert (clamp 3.0 5.0 0.0 == 3.0)
}
