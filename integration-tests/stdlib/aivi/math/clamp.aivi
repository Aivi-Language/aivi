@no_prelude
module integrationTests.stdlib.aivi.math.clamp

use aivi
use aivi.testing (assert)

use aivi.math

subject = clamp

@test
smoke = effect {
  _ <- assert (clamp 10.0 5.0 0.0 == 5.0)
  _ <- assert (clamp (-5.0) 5.0 0.0 == 0.0)
  _ <- assert (clamp 3.0 5.0 0.0 == 3.0)
}
