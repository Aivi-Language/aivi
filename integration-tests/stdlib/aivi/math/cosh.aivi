@no_prelude
module integrationTests.stdlib.aivi.math.cosh

use aivi
use aivi.testing
use aivi.math

@test
smoke = effect {
  _ <- assert (abs (cosh 0.0-1.0) < 0.0001)
  _ <- assert (abs (cosh 1.0-1.5430) < 0.0001)
}
