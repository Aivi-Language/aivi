@no_prelude
module integrationTests.stdlib.aivi.math.expm1

use aivi
use aivi.testing (assert)

use aivi.math

subject = expm1

@test
smoke = effect {
  _ <- assert (abs (expm1 0.0) < 0.0001)
  // expm1(x) = exp(x) - 1
  _ <- assert (abs (expm1 1.0- (e -1.0)) < 0.0001)
}
