@no_prelude
module integrationTests.stdlib.aivi.math.expm1

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (abs (expm1 0.0) < 0.0001)
  // expm1(x) = exp(x) - 1
  _ <- testing.assert (abs (expm1 1.0- (e -1.0)) < 0.0001)
}
