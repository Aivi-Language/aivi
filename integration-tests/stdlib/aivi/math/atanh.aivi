@no_prelude
module integrationTests.stdlib.aivi.math.atanh

use aivi
use aivi.testing (assert)

use aivi.math

subject = atanh

@test
smoke = effect {
  _ <- assert (abs (atanh 0.0) < 0.0001)
  _ <- assert (abs (atanh 0.7615-1.0) < 0.0001)
}
