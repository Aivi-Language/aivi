@no_prelude
module integrationTests.stdlib.aivi.math.cbrt

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (abs (cbrt 8.0-2.0) < 0.0001)
  _ <- testing.assert (abs (cbrt 27.0-3.0) < 0.0001)
}
