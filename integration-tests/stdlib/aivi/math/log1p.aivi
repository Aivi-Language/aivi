@no_prelude
module integrationTests.stdlib.aivi.math.log1p

use aivi
use aivi.testing

use aivi.math

subject = log1p

@test
smoke = effect {
  _ <- testing.assert (abs (log1p 0.0) < 0.0001)
  // log1p(x) = log(1+x)
  _ <- testing.assert (abs (log1p (e -1.0) -1.0) < 0.0001)
}
