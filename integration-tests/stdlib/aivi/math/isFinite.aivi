@no_prelude
module integrationTests.stdlib.aivi.math.isFinite

use aivi
use aivi.testing

use aivi.math

subject = isFinite

@test
smoke = effect {
  _ <- testing.assert (isFinite 1.0 == True)
  _ <- testing.assert (isFinite inf == False)
  _ <- testing.assert (isFinite nan == False)
}
