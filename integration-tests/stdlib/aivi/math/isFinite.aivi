@no_prelude
module integrationTests.stdlib.aivi.math.isFinite

use aivi
use aivi.testing
use aivi.math

@test
smoke = effect {
  _ <- assert (isFinite 1.0 == True)
  _ <- assert (isFinite inf == False)
  _ <- assert (isFinite nan == False)
}
