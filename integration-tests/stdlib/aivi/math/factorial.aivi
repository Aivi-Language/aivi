@no_prelude
module integrationTests.stdlib.aivi.math.factorial

use aivi
use aivi.testing

use aivi.math

subject = factorial

@test
smoke = effect {
  _ <- testing.assert (factorial 0 == 1)
  _ <- testing.assert (factorial 5 == 120)
}
