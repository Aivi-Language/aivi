@no_prelude
module integrationTests.stdlib.aivi.math.factorial

use aivi
use aivi.testing (assert)

use aivi.math

subject = factorial

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
