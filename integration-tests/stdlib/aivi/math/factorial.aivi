@no_prelude
module integrationTests.stdlib.aivi.math.factorial

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- assert (factorial 0 == 1)
  _ <- assert (factorial 5 == 120)
}
