@no_prelude
module integrationTests.stdlib.aivi.math.acos

use aivi
use aivi.testing
use aivi.math

@test
smoke = effect {
  _ <- assert (abs (acos 1.0).radians < 0.0001)
  _ <- assert (abs (acos 0.0).radians - (pi / 2.0) < 0.0001)
}
