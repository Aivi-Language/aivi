@no_prelude
module integrationTests.stdlib.aivi.math.sin

use aivi
use aivi.testing (assert)

use aivi.math

subject = sin

@test
smoke = effect {
  _ <- assert (abs (sin 0.0) < 0.0001)
  _ <- assert (abs (sin (pi / 2.0) -1.0) < 0.0001)
}
