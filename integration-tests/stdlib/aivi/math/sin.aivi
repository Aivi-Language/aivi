@no_prelude
module integrationTests.stdlib.aivi.math.sin

use aivi
use aivi.testing

use aivi.math

subject = sin

@test
smoke = effect {
  _ <- testing.assert (abs (sin 0.0) < 0.0001)
  _ <- testing.assert (abs (sin (pi / 2.0) -1.0) < 0.0001)
}
