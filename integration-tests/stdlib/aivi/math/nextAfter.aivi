@no_prelude
module integrationTests.stdlib.aivi.math.nextAfter

use aivi
use aivi.testing (assert)

use aivi.math

subject = nextAfter

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
