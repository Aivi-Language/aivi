@no_prelude
module integrationTests.stdlib.aivi.math.exp

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- assert (abs (exp 0.0-1.0) < 0.0001)
  _ <- assert (abs (exp 1.0- e) < 0.0001)
}
