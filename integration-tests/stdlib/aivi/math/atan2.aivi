@no_prelude
module integrationTests.stdlib.aivi.math.atan2

use aivi
use aivi.testing

use aivi.math

subject = atan2

@test
smoke = effect {
  _ <- testing.assert (abs (atan2 1.0 1.0).radians - (pi / 4.0) < 0.0001)
  _ <- testing.assert (abs (atan2 1.0 0.0).radians - (pi / 2.0) < 0.0001)
}
