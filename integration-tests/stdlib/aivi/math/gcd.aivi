@no_prelude
module integrationTests.stdlib.aivi.math.gcd

use aivi
use aivi.testing (assert)

use aivi.math

subject = gcd

@test
smoke = effect {
  _ <- assert (gcd 12 8 == 4)
  _ <- assert (gcd 17 5 == 1)
}
