@no_prelude
module integrationTests.stdlib.aivi.math.sqrt

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (abs (sqrt 4.0-2.0) < 0.0001)
  _ <- testing.assert (abs (sqrt 2.0-1.4142) < 0.0001)
}
