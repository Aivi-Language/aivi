@no_prelude
module integrationTests.stdlib.aivi.math.sqrt

use aivi
use aivi.testing (assert)

use aivi.math

subject = sqrt

@test
smoke = effect {
  _ <- assert (abs (sqrt 4.0-2.0) < 0.0001)
  _ <- assert (abs (sqrt 2.0-1.4142) < 0.0001)
}
