@no_prelude
module integrationTests.stdlib.aivi.math.pow

use aivi
use aivi.testing

use aivi.math

subject = pow

@test
smoke = effect {
  _ <- testing.assert (pow 2.0 3.0 == 8.0)
  _ <- testing.assert (pow 3.0 2.0 == 9.0)
}
