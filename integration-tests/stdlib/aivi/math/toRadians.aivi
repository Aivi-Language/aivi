@no_prelude
module integrationTests.stdlib.aivi.math.toRadians

use aivi
use aivi.testing (assert)

use aivi.math

subject = toRadians

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
