@no_prelude
module integrationTests.stdlib.aivi.math.hypot

use aivi
use aivi.testing

use aivi.math

subject = hypot

@test
smoke = effect {
  _ <- testing.assert (abs (hypot 3.0 4.0-5.0) < 0.0001)
}
