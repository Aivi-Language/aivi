@no_prelude
module integrationTests.stdlib.aivi.math.tanh

use aivi
use aivi.testing

use aivi.math

subject = tanh

@test
smoke = effect {
  _ <- testing.assert (abs (tanh 0.0) < 0.0001)
  _ <- testing.assert (abs (tanh 1.0-0.7615) < 0.0001)
}
