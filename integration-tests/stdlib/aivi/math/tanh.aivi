@no_prelude
module integrationTests.stdlib.aivi.math.tanh

use aivi
use aivi.testing
use aivi.math

@test
smoke = effect {
  _ <- assert (abs (tanh 0.0) < 0.0001)
  _ <- assert (abs (tanh 1.0-0.7615) < 0.0001)
}
