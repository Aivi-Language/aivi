@no_prelude
module integrationTests.stdlib.aivi.math.log

use aivi
use aivi.testing (assert)

use aivi.math

subject = log

@test
smoke = effect {
  _ <- assert (abs (log e -1.0) < 0.0001)
  _ <- assert (abs (log 1.0) < 0.0001)
}
