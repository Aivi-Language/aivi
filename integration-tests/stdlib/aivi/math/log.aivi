@no_prelude
module integrationTests.stdlib.aivi.math.log

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (abs (log e -1.0) < 0.0001)
  _ <- testing.assert (abs (log 1.0) < 0.0001)
}
