@no_prelude
module integrationTests.stdlib.aivi.math.ceil

use aivi
use aivi.testing (assert)

use aivi.math

subject = ceil

@test
smoke = effect {
  _ <- assert (ceil 1.2 == 2.0)
  _ <- assert (ceil 1.9 == 2.0)
  _ <- assert (ceil (-1.2) == -1.0)
}
