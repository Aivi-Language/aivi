@no_prelude
module integrationTests.stdlib.aivi.math.toDegrees

use aivi
use aivi.testing (assert)

use aivi.math

subject = toDegrees

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
