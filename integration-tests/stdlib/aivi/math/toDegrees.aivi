@no_prelude
module integrationTests.stdlib.aivi.math.toDegrees

use aivi
use aivi.testing
use aivi.math

@test
smoke = effect {
  a = { radians: pi }
  _ <- assert (abs (toDegrees a - 180.0) < 1e-10)
}
