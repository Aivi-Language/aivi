@no_prelude
module integrationTests.stdlib.aivi.math.radians

use aivi
use aivi.testing

use aivi.math

subject = radians

@test
smoke = effect {
  _ <- testing.assert (abs (radians 180.0- pi) < 0.0001)
}
