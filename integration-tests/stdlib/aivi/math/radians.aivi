@no_prelude
module integrationTests.stdlib.aivi.math.radians

use aivi
use aivi.testing (assert)

use aivi.math

subject = radians

@test
smoke = effect {
  _ <- assert (abs (radians 180.0- pi) < 0.0001)
}
