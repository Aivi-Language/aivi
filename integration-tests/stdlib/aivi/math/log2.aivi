@no_prelude
module integrationTests.stdlib.aivi.math.log2

use aivi
use aivi.testing (assert)

use aivi.math

subject = log2

@test
smoke = effect {
  _ <- assert (abs (log2 8.0-3.0) < 0.0001)
  _ <- assert (abs (log2 1.0) < 0.0001)
}
