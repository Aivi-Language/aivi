@no_prelude
module integrationTests.stdlib.aivi.math.modf

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  (i, f) = modf 1.5
  _ <- testing.assert (i == 1.0)
  _ <- testing.assert (abs (f -0.5) < 0.0001)
}
