@no_prelude
module integrationTests.stdlib.aivi.math.acosh

use aivi
use aivi.testing (assert)

use aivi.math

subject = acosh

@test
smoke = effect {
  _ <- assert (abs (acosh 1.0) < 0.0001)
  _ <- assert (abs (acosh 1.5430-1.0) < 0.0001)
}
