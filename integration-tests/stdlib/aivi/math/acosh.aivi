@no_prelude
module integrationTests.stdlib.aivi.math.acosh

use aivi
use aivi.testing

use aivi.math

subject = acosh

@test
smoke = effect {
  _ <- testing.assert (abs (acosh 1.0) < 0.0001)
  _ <- testing.assert (abs (acosh 1.5430-1.0) < 0.0001)
}
