@no_prelude
module integrationTests.stdlib.aivi.math.log10

use aivi
use aivi.testing

use aivi.math

subject = log10

@test
smoke = effect {
  _ <- testing.assert (abs (log10 100.0-2.0) < 0.0001)
  _ <- testing.assert (abs (log10 1.0) < 0.0001)
}
