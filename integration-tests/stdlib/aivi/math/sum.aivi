@no_prelude
module integrationTests.stdlib.aivi.math.sum

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (sum [1.0, 2.0, 3.0] == 6.0)
  _ <- testing.assert (sum [] == 0.0)
}
