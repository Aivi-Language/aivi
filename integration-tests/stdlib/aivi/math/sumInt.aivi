@no_prelude
module integrationTests.stdlib.aivi.math.sumInt

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (sumInt [1, 2, 3] == 6)
  _ <- testing.assert (sumInt [] == 0)
}
