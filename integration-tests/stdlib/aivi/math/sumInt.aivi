@no_prelude
module integrationTests.stdlib.aivi.math.sumInt

use aivi
use aivi.testing (assert)

use aivi.math

subject = sumInt

@test
smoke = effect {
  _ <- assert (sumInt [1, 2, 3] == 6)
  _ <- assert (sumInt [] == 0)
}
