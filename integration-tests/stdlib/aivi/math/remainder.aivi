@no_prelude
module integrationTests.stdlib.aivi.math.remainder

use aivi
use aivi.testing (assert)

use aivi.math

subject = remainder

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
