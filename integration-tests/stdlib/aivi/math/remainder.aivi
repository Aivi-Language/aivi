@no_prelude
module integrationTests.stdlib.aivi.math.remainder

use aivi
use aivi.testing

use aivi.math

subject = remainder

@test
smoke = effect {
  _ <- testing.assert (abs (remainder 5.5 2.0- (-0.5)) < 0.0001)
  // 5.5 - 6.0 = -0.5
}
