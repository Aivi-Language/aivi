@no_prelude
module integrationTests.stdlib.aivi.math.round

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (round 1.2 == 1.0)
  _ <- testing.assert (round 1.8 == 2.0)
  _ <- testing.assert (round 1.5 == 2.0) // Round to nearest even? Check implementation.
  // Rust round() rounds half-way cases away from zero.
  // Wait, looking at math.rs: "if frac.abs() == 0.5 { let even ..." -> It implements round half to even!
  _ <- testing.assert (round 2.5 == 2.0)
  _ <- testing.assert (round 3.5 == 4.0)
}
