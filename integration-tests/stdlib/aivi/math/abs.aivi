@no_prelude
module integrationTests.stdlib.aivi.math.abs

use aivi
use aivi.testing (assert)

use aivi.math

subject = abs

@test
smoke = effect {
  _ <- assert (abs 1 == 1)
  _ <- assert (abs (-1) == 1)
  _ <- assert (abs 1.5 == 1.5)
  _ <- assert (abs (-1.5) == 1.5)
}
