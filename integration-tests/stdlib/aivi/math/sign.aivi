@no_prelude
module integrationTests.stdlib.aivi.math.sign

use aivi
use aivi.testing

use aivi.math

subject = sign

@test
smoke = effect {
  _ <- testing.assert (sign 10.0 == 1.0)
  _ <- testing.assert (sign (-10.0) == -1.0)
  _ <- testing.assert (sign 0.0 == 0.0)
}
