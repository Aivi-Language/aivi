@no_prelude
module integrationTests.stdlib.aivi.math.sign

use aivi
use aivi.testing (assert)

use aivi.math

subject = sign

@test
smoke = effect {
  _ <- assert (sign 10.0 == 1.0)
  _ <- assert (sign (-10.0) == -1.0)
  _ <- assert (sign 0.0 == 0.0)
}
