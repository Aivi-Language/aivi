@no_prelude
module integrationTests.stdlib.aivi.math.trunc

use aivi
use aivi.testing

use aivi.math

subject = trunc

@test
smoke = effect {
  _ <- testing.assert (trunc 1.9 == 1.0)
  _ <- testing.assert (trunc (-1.9) == -1.0)
}
