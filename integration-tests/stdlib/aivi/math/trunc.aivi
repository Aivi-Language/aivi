@no_prelude
module integrationTests.stdlib.aivi.math.trunc

use aivi
use aivi.testing (assert)

use aivi.math

subject = trunc

@test
smoke = effect {
  _ <- assert (trunc 1.9 == 1.0)
  _ <- assert (trunc (-1.9) == -1.0)
}
