@no_prelude
module integrationTests.stdlib.aivi.math.degrees

use aivi
use aivi.testing

use aivi.math

subject = degrees

@test
smoke = effect {
  _ <- testing.assert (abs (degrees pi -180.0) < 0.0001)
}
