@no_prelude
module integrationTests.stdlib.aivi.math.divmod

use aivi
use aivi.testing
use aivi.math

@test
smoke = effect {
  _ <- assert (divmod 10 3 == (3, 1))
  _ <- assert (divmod 10 (-3) == (-4, -2))
}
