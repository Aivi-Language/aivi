@no_prelude
module integrationTests.stdlib.aivi.math.atan

use aivi
use aivi.testing

use aivi.math



@test
smoke = effect {
  _ <- testing.assert (abs (atan 0.0).radians < 0.0001)
  _ <- testing.assert (abs (atan 1.0).radians - (pi / 4.0) < 0.0001)
}
