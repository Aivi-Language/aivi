@no_prelude
module integrationTests.stdlib.aivi.math.atan

use aivi
use aivi.testing (assert)

use aivi.math

subject = atan

@test
smoke = effect {
  _ <- assert (abs (atan 0.0).radians < 0.0001)
  _ <- assert (abs (atan 1.0).radians - (pi / 4.0) < 0.0001)
}
