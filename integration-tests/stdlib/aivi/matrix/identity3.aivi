@no_prelude
module integrationTests.stdlib.aivi.matrix.identity3

use aivi
use aivi.testing (assert)

use aivi.matrix

subject = identity3

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
