@no_prelude
module integrationTests.stdlib.aivi.matrix.identity3

use aivi
use aivi.testing

use aivi.matrix



@test
smoke = effect {
  _ <- testing.assert (identity3.m00 == 1.0)
  _ <- testing.assert (identity3.m22 == 1.0)
}
