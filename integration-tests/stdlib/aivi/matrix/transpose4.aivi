@no_prelude
module integrationTests.stdlib.aivi.matrix.transpose4

use aivi
use aivi.testing

use aivi.matrix

subject = transpose4

@test
smoke = effect {
  _ <- testing.assert (transpose4 identity4 == identity4)
}
