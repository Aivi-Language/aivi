@no_prelude
module integrationTests.stdlib.aivi.matrix.transpose4

use aivi
use aivi.testing (assert)

use aivi.matrix

subject = transpose4

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
