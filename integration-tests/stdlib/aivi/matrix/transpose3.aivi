@no_prelude
module integrationTests.stdlib.aivi.matrix.transpose3

use aivi
use aivi.testing

use aivi.matrix

subject = transpose3

@test
smoke = effect {
  _ <- testing.assert (transpose3 identity3 == identity3)
}
