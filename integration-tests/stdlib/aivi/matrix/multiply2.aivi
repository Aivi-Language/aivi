@no_prelude
module integrationTests.stdlib.aivi.matrix.multiply2

use aivi
use aivi.testing

use aivi.matrix

subject = multiply2

@test
smoke = effect {
  _ <- testing.assert (multiply2 identity2 identity2 == identity2)
}
