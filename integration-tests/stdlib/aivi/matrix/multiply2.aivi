@no_prelude
module integrationTests.stdlib.aivi.matrix.multiply2

use aivi
use aivi.testing (assert)

use aivi.matrix

subject = multiply2

@test
smoke = effect {
  _ <- assert (multiply2 identity2 identity2 == identity2)
}
