@no_prelude
module integrationTests.stdlib.aivi.matrix.identity2

use aivi
use aivi.testing (assert)

use aivi.matrix

subject = identity2

@test
smoke = effect {
  _ <- assert (identity2.m00 == 1.0)
  _ <- assert (identity2.m01 == 0.0)
}
