@no_prelude
module integrationTests.stdlib.aivi.matrix.identity2

use aivi
use aivi.testing (assert)

use aivi.matrix

subject = identity2

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
