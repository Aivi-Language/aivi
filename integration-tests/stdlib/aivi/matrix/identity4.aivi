@no_prelude
module integrationTests.stdlib.aivi.matrix.identity4

use aivi
use aivi.testing (assert)

use aivi.matrix

subject = identity4

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
