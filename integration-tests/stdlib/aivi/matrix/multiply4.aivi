@no_prelude
module integrationTests.stdlib.aivi.matrix.multiply4

use aivi
use aivi.testing

use aivi.matrix



@test
smoke = effect {
  _ <- testing.assert (multiply4 identity4 identity4 == identity4)
}
