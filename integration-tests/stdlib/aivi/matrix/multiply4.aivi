@no_prelude
module integrationTests.stdlib.aivi.matrix.multiply4

use aivi
use aivi.testing (assert)

use aivi.matrix

subject = multiply4

@test
smoke = effect {
  _ <- assert (multiply4 identity4 identity4 == identity4)
}
