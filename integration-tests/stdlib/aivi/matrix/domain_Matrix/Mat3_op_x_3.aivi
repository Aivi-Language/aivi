@no_prelude
module integrationTests.stdlib.aivi.matrix.domain_Matrix.Mat3_op_x_3

use aivi
use aivi.testing (assert)

use aivi.matrix

use aivi.matrix (domain Matrix)

left = { m00: 1.0, m01: 1.0, m02: 1.0, m10: 1.0, m11: 1.0, m12: 1.0, m20: 1.0, m21: 1.0, m22: 1.0 }
right = Unit

subject = left * right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
