@no_prelude
module integrationTests.stdlib.aivi.matrix.multiply3

use aivi
use aivi.testing

use aivi.matrix

subject = multiply3

@test
smoke = effect {
  _ <- testing.assert (multiply3 identity3 identity3 == identity3)
}
