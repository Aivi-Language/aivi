@no_prelude
module integrationTests.stdlib.aivi.matrix.transpose2

use aivi
use aivi.testing (assert)

use aivi.matrix

subject = transpose2

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
