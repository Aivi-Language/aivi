@no_prelude
module integrationTests.stdlib.aivi.regex.replaceAll

use aivi
use aivi.testing (assert)

use aivi.regex

subject = replaceAll

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
