@no_prelude
module integrationTests.stdlib.aivi.regex.RegexError

use aivi
use aivi.testing (assert)

use aivi.regex

Ref = RegexError

@test
smoke = effect {
  _ <- match compile "[" {
    | Ok _ => assert False
    | Err _ => assert True // Err holds RegexError
  }
}
