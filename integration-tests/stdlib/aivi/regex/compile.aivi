@no_prelude
module integrationTests.stdlib.aivi.regex.compile

use aivi
use aivi.testing
use aivi.regex

@test
smoke = effect {
  reResult = compile "abc"
  _ <- reResult ?
    | Ok _  => assert True
    | Err _ => assert False
}
