@no_prelude
module integrationTests.stdlib.aivi.regex.compile

use aivi
use aivi.testing (assert)

use aivi.regex

subject = compile

@test
smoke = effect {
  _ <- match compile "abc" {
    | Ok _ => assert True
    | Err _ => assert False
  }
}
