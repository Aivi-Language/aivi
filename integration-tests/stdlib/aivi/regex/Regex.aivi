@no_prelude
module integrationTests.stdlib.aivi.regex.Regex

use aivi
use aivi.testing (assert)

use aivi.regex

Ref = Regex

@test
smoke = effect {
  reResult = compile "."
  _ <- reResult ?
    | Ok re => assert True
    | Err _ => assert False
}
