@no_prelude
module integrationTests.stdlib.aivi.regex.Regex

use aivi
use aivi.testing (assert)

use aivi.regex

Ref = Regex

@test
smoke = effect {
  _ <- match compile "." {
    | Ok re => assert True // re has type Regex
    | Err _ => assert False
  }
}
