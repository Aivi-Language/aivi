@no_prelude
module integrationTests.stdlib.aivi.regex.Regex

use aivi
use aivi.testing

use aivi.regex



@test
smoke = effect {
  reResult = compile "."
  _ <- reResult ?
    | Ok re => assert True
    | Err _ => assert False
}
