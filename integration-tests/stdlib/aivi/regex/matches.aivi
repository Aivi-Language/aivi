@no_prelude
module integrationTests.stdlib.aivi.regex.matches

use aivi
use aivi.testing
use aivi.regex

@test
smoke = effect {
  reResult = compile "l"
  r <- reResult ?
    | Ok re => pure re
    | Err _ => fail "compile failed"
    ms = matches r "hello"
    _ <- assert (List.length ms == 2)
}
