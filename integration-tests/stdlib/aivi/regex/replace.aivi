@no_prelude
module integrationTests.stdlib.aivi.regex.replace

use aivi
use aivi.testing (assert)

use aivi.regex

subject = replace

@test
smoke = effect {
  r <- match compile "l" {
    | Ok re => pure re
    | Err _ => fail "compile failed"
  }
  _ <- assert (replace r "hello" "L" == "heLlo")
}
