@no_prelude
module integrationTests.stdlib.aivi.Bool

use aivi
use aivi.testing
use aivi

@test
smoke = effect {
  assert (True && True)
  assert (True || False)
  assert (False || True)
  assert (!False)
  assert (!(True && False))
  assert (True != False)
  assert (True == True)
}
