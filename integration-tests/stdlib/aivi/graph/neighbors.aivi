@no_prelude
module integrationTests.stdlib.aivi.graph.neighbors

use aivi
use aivi.testing (assert)

use aivi.graph

subject = neighbors

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
