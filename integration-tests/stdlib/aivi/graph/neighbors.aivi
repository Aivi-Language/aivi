@no_prelude
module integrationTests.stdlib.aivi.graph.neighbors

use aivi
use aivi.testing

use aivi.graph



@test
smoke = effect {
  g = addEdge empty { from: 1, to: 2, weight: 1.0 }
  ns = neighbors g 1
  _ <- assert (ns == [2])
}
