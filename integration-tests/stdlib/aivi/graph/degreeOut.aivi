@no_prelude
module integrationTests.stdlib.aivi.graph.degreeOut

use aivi
use aivi.testing (assert)

use aivi.graph

subject = degreeOut

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
