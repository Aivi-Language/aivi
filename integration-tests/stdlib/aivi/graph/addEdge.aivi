@no_prelude
module integrationTests.stdlib.aivi.graph

use aivi
use aivi.testing (assert)

use aivi.graph

@test
smoke = effect {
  g = addEdge empty { from: 1, to: 2, weight: 1.0 }
  // addEdge ensures nodes exist
  _ <- assert (length g.nodes == 2)
  _ <- assert (length g.edges == 1)
}
