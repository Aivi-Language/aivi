@no_prelude
module integrationTests.stdlib.aivi.graph.dfs

use aivi
use aivi.testing
use aivi.graph

@test
smoke = effect {
  g = empty |> addNode 1 |> addNode 2 |> addNode 3 |> addEdge 1 2 |> addEdge 2 3
  _ <- assert (List.length (dfs g 1) == 3)
}
