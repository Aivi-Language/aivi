@no_prelude
module integrationTests.stdlib.aivi.graph.Edge

use aivi
use aivi.testing
use aivi.graph


touch : Edge -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
