@no_prelude
module integrationTests.stdlib.aivi.graph.shortestPath

use aivi
use aivi.testing (assert)

use aivi.graph

subject = shortestPath

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
