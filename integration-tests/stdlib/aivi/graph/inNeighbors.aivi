@no_prelude
module integrationTests.stdlib.aivi.graph.inNeighbors

use aivi
use aivi.testing (assert)

use aivi.graph

subject = inNeighbors

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
