@no_prelude
module integrationTests.stdlib.aivi.graph.edgesFrom

use aivi
use aivi.testing (assert)

use aivi.graph

subject = edgesFrom

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
