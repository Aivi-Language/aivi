@no_prelude
module integrationTests.stdlib.aivi.graph.hasCycle

use aivi
use aivi.testing (assert)

use aivi.graph

subject = hasCycle

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
