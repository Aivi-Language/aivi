@no_prelude
module integrationTests.stdlib.aivi.graph.addNode

use aivi
use aivi.testing

use aivi.graph

subject = addNode

@test
smoke = effect {
  g = addNode empty 1
  _ <- testing.assert (length g.nodes == 1)
}
