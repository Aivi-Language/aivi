@no_prelude
module integrationTests.stdlib.aivi.graph.addNode

use aivi
use aivi.testing (assert)

use aivi.graph

subject = addNode

@test
smoke = effect {
  g = addNode empty 1
  _ <- assert (length g.nodes == 1)
}
