@no_prelude
module integrationTests.stdlib.aivi.graph.empty

use aivi
use aivi.testing (assert)

use aivi.graph

subject = empty

@test
smoke = effect {
  g = empty
  _ <- assert (length g.nodes == 0)
}
