@no_prelude
module integrationTests.stdlib.aivi.graph.empty

use aivi
use aivi.testing

use aivi.graph

subject = empty

@test
smoke = effect {
  g = empty
  _ <- testing.assert (length g.nodes == 0)
}
