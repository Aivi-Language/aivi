@no_prelude
module integrationTests.stdlib.aivi.graph.dedupEdges

use aivi
use aivi.testing (assert)

use aivi.graph

subject = dedupEdges

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
