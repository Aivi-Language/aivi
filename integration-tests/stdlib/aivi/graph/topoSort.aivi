@no_prelude
module integrationTests.stdlib.aivi.graph.topoSort

use aivi
use aivi.testing
use aivi.graph

@test
smoke = effect {
  g = empty |> addNode 1 |> addNode 2 |> addNode 3 |> addEdge 1 2 |> addEdge 2 3
  _ <- assert (topoSort g == Ok [1, 2, 3])
}
