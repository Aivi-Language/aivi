@no_prelude
module integrationTests.stdlib.aivi.graph.topoSort

use aivi
use aivi.testing (assert)

use aivi.graph

subject = topoSort

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
