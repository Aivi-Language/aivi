@no_prelude
module integrationTests.stdlib.aivi.graph.fromWeightedEdges

use aivi
use aivi.testing

use aivi.graph

subject = fromWeightedEdges

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
