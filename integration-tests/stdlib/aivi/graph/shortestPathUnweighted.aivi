@no_prelude
module integrationTests.stdlib.aivi.graph.shortestPathUnweighted

use aivi
use aivi.testing
use aivi.graph

@test
smoke = effect {
  g = empty |> addNode 1 |> addNode 2 |> addNode 3 |> addEdge 1 2 |> addEdge 2 3
  _ <- assert (shortestPathUnweighted g 1 3 == Ok [1, 2, 3])
}
