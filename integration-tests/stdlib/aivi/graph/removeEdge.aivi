@no_prelude
module integrationTests.stdlib.aivi.graph.removeEdge

use aivi
use aivi.testing (assert)

use aivi.graph

subject = removeEdge

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
