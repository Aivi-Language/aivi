@no_prelude
module integrationTests.stdlib.aivi.graph.bfs

use aivi
use aivi.testing (assert)

use aivi.graph

subject = bfs

@test
smoke = effect {
  g = addEdge empty { from: 1, to: 2, weight: 1.0 }
  path = bfs g { start: 1, end: 2 }
  _ <- assert (path == [1, 2])
}
