@no_prelude
module integrationTests.stdlib.aivi.url

use aivi
use aivi.testing
use aivi.url

@test
smoke = effect {
  u = parse "https://example.com/path?q=1"
  assert (toString u == "https://example.com/path?q=1")
}
