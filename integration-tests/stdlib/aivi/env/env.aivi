@no_prelude
module integrationTests.stdlib.aivi.env.tests

use aivi
use aivi.testing
use aivi.env
use aivi.text

@test
get_smoke = effect {
  src = env.get "PATH"
  src ?
    | Some path => assert (text.isEmpty path == False)
    | None      => assert True
}
