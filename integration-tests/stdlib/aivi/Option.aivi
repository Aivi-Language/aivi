@no_prelude
module integrationTests.stdlib.aivi.Option

use aivi
use aivi.testing
use aivi.logic

@test
smoke = effect {
  val = Some 1
  _ <- val ?
    | Some _ => testing.assert True
    | None => testing.assert False
}
