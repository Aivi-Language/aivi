@no_prelude
module integrationTests.stdlib.aivi.Option

use aivi
use aivi.testing
use aivi.logic

@test
smoke = effect {
  val = Some 1
  val ?
    | Some _ => assert True
    | None   => assert False
}
