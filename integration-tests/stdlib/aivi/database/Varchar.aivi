@no_prelude
module integrationTests.stdlib.aivi.database.Varchar

use aivi
use aivi.testing (assert)

use aivi.database

subject = Varchar

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
