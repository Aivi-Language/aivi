@no_prelude
module integrationTests.stdlib.aivi.database.AutoIncrement

use aivi
use aivi.testing
use aivi.database

touch : AutoIncrement -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
