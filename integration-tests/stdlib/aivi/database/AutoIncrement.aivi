@no_prelude
module integrationTests.stdlib.aivi.database.AutoIncrement

use aivi
use aivi.testing (assert)

use aivi.database

subject = AutoIncrement

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
