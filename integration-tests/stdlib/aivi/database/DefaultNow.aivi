@no_prelude
module integrationTests.stdlib.aivi.database.DefaultNow

use aivi
use aivi.testing (assert)

use aivi.database

subject = DefaultNow

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
