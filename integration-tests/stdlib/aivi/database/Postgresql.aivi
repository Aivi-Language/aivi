@no_prelude
module integrationTests.stdlib.aivi.database.Postgresql

use aivi
use aivi.testing (assert)

use aivi.database

subject = Postgresql

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
