@no_prelude
module integrationTests.stdlib.aivi.database.Delta

use aivi
use aivi.testing (assert)

use aivi.database

subject = Delta

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
