@no_prelude
module integrationTests.stdlib.aivi.database.table

use aivi
use aivi.testing (assert)

use aivi.database

subject = table

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
