@no_prelude
module integrationTests.stdlib.aivi.database.Mysql

use aivi
use aivi.testing
use aivi.database


touch : Mysql -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
