@no_prelude
module integrationTests.stdlib.aivi.database.Mysql

use aivi
use aivi.testing (assert)

use aivi.database

subject = Mysql

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
