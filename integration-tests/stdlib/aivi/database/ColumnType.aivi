@no_prelude
module integrationTests.stdlib.aivi.database.ColumnType

use aivi
use aivi.testing
use aivi.database


touch : ColumnType -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
