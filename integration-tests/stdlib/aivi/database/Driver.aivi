@no_prelude
module integrationTests.stdlib.aivi.database.Driver

use aivi
use aivi.testing
use aivi.database


touch : Driver -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
