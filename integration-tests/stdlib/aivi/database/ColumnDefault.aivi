@no_prelude
module integrationTests.stdlib.aivi.database.ColumnDefault

use aivi
use aivi.testing
use aivi.database


touch : ColumnDefault -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
