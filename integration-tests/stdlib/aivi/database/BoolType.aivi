@no_prelude
module integrationTests.stdlib.aivi.database.BoolType

use aivi
use aivi.testing (assert)

use aivi.database

subject = BoolType

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
