@no_prelude
module integrationTests.stdlib.aivi.database.BoolType

use aivi
use aivi.testing
use aivi.database


touch : BoolType -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
