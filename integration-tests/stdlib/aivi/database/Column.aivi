@no_prelude
module integrationTests.stdlib.aivi.database.Column

use aivi
use aivi.testing

use aivi.database



@test
smoke = effect {
  c = { name: "id", colType: IntType, constraints: [NotNull, AutoIncrement], default: None }
  _ <- testing.assert (c.name == "id")
}
