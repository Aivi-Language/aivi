@no_prelude
module integrationTests.stdlib.aivi.database.Column

use aivi
use aivi.testing (assert)

use aivi.database

Ref = Column

@test
smoke = effect {
  c = { name: "id", type: IntType, constraints: [NotNull, AutoIncrement], default: None }
  _ <- assert (c.name == "id")
}
