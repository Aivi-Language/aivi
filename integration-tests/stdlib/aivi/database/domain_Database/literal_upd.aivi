@no_prelude
module integrationTests.stdlib.aivi.database.domain_Database.literal_upd

use aivi
use aivi.testing
use aivi.database
use aivi.database

@test
smoke = effect {
  _ <- pure literal_upd
  _ <- assertEq (1 + 1) 2
}
