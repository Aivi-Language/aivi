@no_prelude
module integrationTests.stdlib.aivi.database.pool.Timeout

use aivi
use aivi.testing (assert)

use aivi.database.pool

subject = Timeout

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
