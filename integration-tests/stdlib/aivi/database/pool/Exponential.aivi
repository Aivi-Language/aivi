@no_prelude
module integrationTests.stdlib.aivi.database.pool.Exponential

use aivi
use aivi.testing
use aivi.database.pool


touch : Exponential -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
