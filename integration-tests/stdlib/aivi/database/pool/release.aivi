@no_prelude
module integrationTests.stdlib.aivi.database.pool.release

use aivi
use aivi.testing (assert)

use aivi.database.pool

subject = release

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
