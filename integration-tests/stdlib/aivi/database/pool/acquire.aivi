@no_prelude
module integrationTests.stdlib.aivi.database.pool.acquire

use aivi
use aivi.testing

use aivi.database.pool

subject = acquire

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
