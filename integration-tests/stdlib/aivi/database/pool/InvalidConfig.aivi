@no_prelude
module integrationTests.stdlib.aivi.database.pool.InvalidConfig

use aivi
use aivi.testing (assert)

use aivi.database.pool

subject = InvalidConfig

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
