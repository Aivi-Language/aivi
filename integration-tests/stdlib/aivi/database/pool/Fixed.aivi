@no_prelude
module integrationTests.stdlib.aivi.database.pool.Fixed

use aivi
use aivi.testing
use aivi.database.pool


touch : Fixed -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
