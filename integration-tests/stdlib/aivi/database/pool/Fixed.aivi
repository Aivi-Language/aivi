@no_prelude
module integrationTests.stdlib.aivi.database.pool.Fixed

use aivi
use aivi.testing (assert)

use aivi.database.pool

subject = Fixed

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
