@no_prelude
module integrationTests.stdlib.aivi.database.pool.Closed

use aivi
use aivi.testing (assert)

use aivi.database.pool

subject = Closed

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
