@no_prelude
module integrationTests.stdlib.aivi.database.pool.BackoffPolicy

use aivi
use aivi.testing
use aivi.database.pool


touch : BackoffPolicy -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
