@no_prelude
module integrationTests.stdlib.aivi.database.pool.Pool

use aivi
use aivi.testing
use aivi.database.pool

touch : Pool -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
