@no_prelude
module integrationTests.stdlib.aivi.database.pool.Pool

use aivi
use aivi.testing (assert)

use aivi.database.pool

subject = Pool

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
