@no_prelude
module integrationTests.stdlib.aivi.database.pool.tests

use aivi
use aivi.testing
use aivi.database.pool

backoffPolicy_touch : BackoffPolicy -> Unit
backoffPolicy_touch = _ => Unit

@test
backoffPolicy_smoke = effect {
  pure backoffPolicy_touch
  assertEq (1 + 1) 2
}

closed_touch : Closed -> Unit
closed_touch = _ => Unit

@test
closed_smoke = effect {
  pure closed_touch
  assertEq (1 + 1) 2
}

config_touch : Config -> Unit
config_touch = _ => Unit

@test
config_smoke = effect {
  pure config_touch
  assertEq (1 + 1) 2
}

exponential_touch : Exponential -> Unit
exponential_touch = _ => Unit

@test
exponential_smoke = effect {
  pure exponential_touch
  assertEq (1 + 1) 2
}

fifo_touch : Fifo -> Unit
fifo_touch = _ => Unit

@test
fifo_smoke = effect {
  pure fifo_touch
  assertEq (1 + 1) 2
}

fixed_touch : Fixed -> Unit
fixed_touch = _ => Unit

@test
fixed_smoke = effect {
  pure fixed_touch
  assertEq (1 + 1) 2
}

healthFailed_touch : HealthFailed -> Unit
healthFailed_touch = _ => Unit

@test
healthFailed_smoke = effect {
  pure healthFailed_touch
  assertEq (1 + 1) 2
}

invalidConfig_touch : InvalidConfig -> Unit
invalidConfig_touch = _ => Unit

@test
invalidConfig_smoke = effect {
  pure invalidConfig_touch
  assertEq (1 + 1) 2
}

lifo_touch : Lifo -> Unit
lifo_touch = _ => Unit

@test
lifo_smoke = effect {
  pure lifo_touch
  assertEq (1 + 1) 2
}

pool_touch : Pool -> Unit
pool_touch = _ => Unit

@test
pool_smoke = effect {
  pure pool_touch
  assertEq (1 + 1) 2
}

poolError_touch : PoolError -> Unit
poolError_touch = _ => Unit

@test
poolError_smoke = effect {
  pure poolError_touch
  assertEq (1 + 1) 2
}

poolStats_touch : PoolStats -> Unit
poolStats_touch = _ => Unit

@test
poolStats_smoke = effect {
  pure poolStats_touch
  assertEq (1 + 1) 2
}

queuePolicy_touch : QueuePolicy -> Unit
queuePolicy_touch = _ => Unit

@test
queuePolicy_smoke = effect {
  pure queuePolicy_touch
  assertEq (1 + 1) 2
}

timeout_touch : Timeout -> Unit
timeout_touch = _ => Unit

@test
timeout_smoke = effect {
  pure timeout_touch
  assertEq (1 + 1) 2
}

@test
acquire_smoke = effect {
  pure acquire
  assertEq (1 + 1) 2
}

@test
close_smoke = effect {
  pure close
  assertEq (1 + 1) 2
}

@test
create_smoke = effect {
  pure create
  assertEq (1 + 1) 2
}

@test
drain_smoke = effect {
  pure drain
  assertEq (1 + 1) 2
}

@test
release_smoke = effect {
  pure release
  assertEq (1 + 1) 2
}

@test
stats_smoke = effect {
  pure stats
  assertEq (1 + 1) 2
}

@test
withConn_smoke = effect {
  pure withConn
  assertEq (1 + 1) 2
}
