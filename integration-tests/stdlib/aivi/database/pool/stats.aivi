@no_prelude
module integrationTests.stdlib.aivi.database.pool.stats

use aivi
use aivi.testing (assert)

use aivi.database.pool

subject = stats

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
