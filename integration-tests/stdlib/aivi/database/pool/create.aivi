@no_prelude
module integrationTests.stdlib.aivi.database.pool.create

use aivi
use aivi.testing (assert)

use aivi.database.pool

subject = create

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
