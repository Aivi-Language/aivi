@no_prelude
module integrationTests.stdlib.aivi.database.pool.close

use aivi
use aivi.testing (assert)

use aivi.database.pool

subject = close

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
