@no_prelude
module integrationTests.stdlib.aivi.database.Sqlite

use aivi
use aivi.testing (assert)

use aivi.database

subject = Sqlite

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
