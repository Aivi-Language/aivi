@no_prelude
module integrationTests.stdlib.aivi.database.Table

use aivi
use aivi.testing

use aivi.database

Ref = Table

@test
smoke = effect {
  t = { name: "users", columns: [], rows: [] }
  _ <- testing.assert (t.name == "users")
}
