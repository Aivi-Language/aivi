@no_prelude
module integrationTests.stdlib.aivi.database.TimestampType

use aivi
use aivi.testing
use aivi.database


touch : TimestampType -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
