@no_prelude
module integrationTests.stdlib.aivi.database.TimestampType

use aivi
use aivi.testing (assert)

use aivi.database

subject = TimestampType

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
