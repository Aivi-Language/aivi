@no_prelude
module integrationTests.stdlib.aivi.database.ColumnConstraint

use aivi
use aivi.testing
use aivi.database


touch : ColumnConstraint -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
