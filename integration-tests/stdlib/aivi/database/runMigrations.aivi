@no_prelude
module integrationTests.stdlib.aivi.database.runMigrations

use aivi
use aivi.testing

use aivi.database

subject = runMigrations

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
