@no_prelude
module integrationTests.stdlib.aivi.database.applyDelta

use aivi
use aivi.testing (assert)

use aivi.database

subject = applyDelta

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
