@no_prelude
module integrationTests.stdlib.aivi.database.NotNull

use aivi
use aivi.testing (assert)

use aivi.database

subject = NotNull

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
