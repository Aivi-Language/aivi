@no_prelude
module integrationTests.stdlib.aivi.path.domain_Path.Path_op_x_1

use aivi
use aivi.testing (assert)

use aivi.path

use aivi.path (domain Path)

left = { absolute: True, segments: [] }
right = { absolute: True, segments: [] }

subject = left / right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
