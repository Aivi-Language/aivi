@no_prelude
module integrationTests.stdlib.aivi.path.normalize

use aivi
use aivi.testing (assert)

use aivi.path

subject = normalize

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
