@no_prelude
module integrationTests.stdlib.aivi.path.normalize

use aivi
use aivi.testing
use aivi.path

@test
smoke = effect {
  p = normalize (parse "/a/b/../c")
  _ <- assert (toString p == "/a/c")
}
