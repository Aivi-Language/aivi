@no_prelude
module integrationTests.stdlib.aivi.path.Path

use aivi
use aivi.testing
use aivi.path


touch : Path -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
