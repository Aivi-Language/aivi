@no_prelude
module integrationTests.stdlib.aivi.path.isAbsolute

use aivi
use aivi.testing
use aivi.path

@test
smoke = effect {
  _ <- assert (isAbsolute (parse "/a"))
  _ <- assert (not (isAbsolute (parse "a")))
}
