@no_prelude
module integrationTests.stdlib.aivi.path.isAbsolute

use aivi
use aivi.testing (assert)

use aivi.path

subject = isAbsolute

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
