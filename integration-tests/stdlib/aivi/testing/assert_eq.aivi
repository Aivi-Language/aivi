@no_prelude
module integrationTests.stdlib.aivi.testing.assert_eq

use aivi
use aivi.testing (assert)

use aivi.testing

subject = assert_eq

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
