@no_prelude
module integrationTests.stdlib.aivi.testing.assertEq

use aivi
use aivi.testing (assert)

use aivi.testing

subject = assertEq

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
