@no_prelude
module integrationTests.stdlib.aivi.testing.tests

use aivi
use aivi.testing

@test
assert_smoke = do Effect {
  assert True
}

@test
assertEq_smoke = do Effect {
  assertEq (1 + 1) 2
}

@test
assert_eq_smoke = do Effect {
  assert_eq (1 + 1) 2
}
