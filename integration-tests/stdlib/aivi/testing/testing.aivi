@no_prelude
module integrationTests.stdlib.aivi.testing.tests

use aivi
use aivi.testing

@test
assert_smoke = effect {
  assert True
}

@test
assertEq_smoke = effect {
  assertEq (1 + 1) 2
}

@test
assert_eq_smoke = effect {
  assert_eq (1 + 1) 2
}
