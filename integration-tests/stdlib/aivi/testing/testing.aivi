@no_prelude
module integrationTests.stdlib.aivi.testing.tests

use aivi
use aivi.testing

@test
assert_smoke = effect {
  pure assert
  assert True
}

@test
assertEq_smoke = effect {
  pure assertEq
  assert True
}

@test
assert_eq_smoke = effect {
  pure assert_eq
  assert True
}
