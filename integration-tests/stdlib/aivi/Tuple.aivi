@no_prelude
module integrationTests.stdlib.aivi.Tuple

use aivi
use aivi.testing
use aivi

@test
smoke = effect {
  t = (1, "two")

  res = t ?
    | (a, b) => if a == 1 && b == "two" then True else False
    | _      => False

    assert (res == True)
}
