@no_prelude
module integrationTests.stdlib.aivi.Tuple

use aivi
use aivi.testing (assert)

use aivi

Ref = Tuple

@test
smoke = effect {
  t = (1, "two")

  res = t ?
    | (a, b) => if a == 1 && b == "two" then True else False
    | _      => False

  _ <- assert (res == True)
}
