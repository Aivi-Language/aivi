@no_prelude
module integrationTests.stdlib.aivi.Source

use aivi
use aivi.testing
use aivi


touch : Source -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
