@no_prelude
module integrationTests.stdlib.aivi.console.tests

use aivi
use aivi.testing
use aivi.console (Blue, Red, Yellow, bgColor, color, error, log, print, println, strip, style)

@test "error"
error_functional = attempt (error "err")
  |> assertEq (Ok Unit)

@test "log"
log_functional = attempt (log "log")
  |> assertEq (Ok Unit)

@test "print"
print_functional = attempt (print "p")
  |> assertEq (Ok Unit)

@test "println"
println_functional = attempt (println "pl")
  |> assertEq (Ok Unit)

@test "color"
color_functional = {
  styled = color Red "warning"
  assert (styled != "warning")
  assert (text.contains styled "[31m")
  assertEq (strip styled) "warning"
}

@test "background color"
bgColor_functional = {
  styled = bgColor Blue "warning"
  assert (styled != "warning")
  assert (text.contains styled "[44m")
  assertEq (strip styled) "warning"
}

@test "style"
style_functional = {
  styled = style {
    fg: Some Yellow
    bg: Some Blue
    bold: True
    dim: False
    italic: True
    underline: True
    blink: False
    inverse: False
    hidden: False
    strike: True
  } "status"
  assert (text.contains styled "[33;44;1;3;4;9m")
  assertEq (strip styled) "status"
}

@test "strip"
strip_functional = {
  styled = color Red "plain"
  assertEq (strip styled) "plain"
}
