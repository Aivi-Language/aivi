@no_prelude
module integrationTests.stdlib.aivi.console.AnsiStyle

use aivi
use aivi.testing
use aivi.console

touch : AnsiStyle -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
