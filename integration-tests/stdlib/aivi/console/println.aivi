@no_prelude
module integrationTests.stdlib.aivi.console.println

use aivi
use aivi.testing (assert)

use aivi.console

subject = println

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
