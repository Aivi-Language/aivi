@no_prelude
module integrationTests.stdlib.aivi.console.bgColor

use aivi
use aivi.testing (assert)

use aivi.console

subject = bgColor

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
