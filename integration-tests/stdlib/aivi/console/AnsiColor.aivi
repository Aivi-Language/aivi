@no_prelude
module integrationTests.stdlib.aivi.console.AnsiColor

use aivi
use aivi.testing
use aivi.console

touch : AnsiColor -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
