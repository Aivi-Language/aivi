@no_prelude
module integrationTests.stdlib.aivi.generator.map

use aivi
use aivi.testing
use aivi.generator

@test
smoke = effect {
  xs = toList (map (_ + 1) (fromList [1, 2, 3]))
  _ <- assert (xs == [2, 3, 4])
}
