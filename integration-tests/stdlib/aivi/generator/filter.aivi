@no_prelude
module integrationTests.stdlib.aivi.generator.filter

use aivi
use aivi.testing (assert)

use aivi.generator

subject = filter

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
