@no_prelude
module integrationTests.stdlib.aivi.generator.filter

use aivi
use aivi.testing
use aivi.generator

@test
smoke = effect {
  xs = toList (filter (_ % 2 == 0) (fromList [1, 2, 3, 4]))
  _ <- assert (xs == [2, 4])
}
