@no_prelude
module integrationTests.stdlib.aivi.generator.fromList

use aivi
use aivi.testing
use aivi.generator

@test
smoke = effect {
  xs = toList (fromList [1, 2, 3])
  _ <- assert (xs == [1, 2, 3])
}
