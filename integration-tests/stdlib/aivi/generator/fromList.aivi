@no_prelude
module integrationTests.stdlib.aivi.generator.fromList

use aivi
use aivi.testing (assert)

use aivi.generator

subject = fromList

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
