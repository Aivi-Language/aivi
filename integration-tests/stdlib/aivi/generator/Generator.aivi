@no_prelude
module integrationTests.stdlib.aivi.generator.Generator

use aivi
use aivi.testing
use aivi.generator

touch : Generator -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
