@no_prelude
module integrationTests.stdlib.aivi.generator.Generator

use aivi
use aivi.testing (assert)

use aivi.generator

subject = Generator

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
