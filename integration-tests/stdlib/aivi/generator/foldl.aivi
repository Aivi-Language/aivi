@no_prelude
module integrationTests.stdlib.aivi.generator.foldl

use aivi
use aivi.testing
use aivi.generator

@test
smoke = effect {
  s = foldl (acc x => acc + x) 0 (fromList [1, 2, 3])
  _ <- assert (s == 6)
}
