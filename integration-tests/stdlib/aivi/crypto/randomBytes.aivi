@no_prelude
module integrationTests.stdlib.aivi.crypto.randomBytes

use aivi
use aivi.testing

use aivi.crypto

subject = randomBytes

@test
smoke = effect {
  b <- randomBytes 16
  _ <- testing.assert (length b == 16)
}
