@no_prelude
module integrationTests.stdlib.aivi.crypto.randomBytes

use aivi
use aivi.testing (assert)

use aivi.crypto

subject = randomBytes

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
