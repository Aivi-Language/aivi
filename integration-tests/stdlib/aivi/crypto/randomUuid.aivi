@no_prelude
module integrationTests.stdlib.aivi.crypto.randomUuid

use aivi
use aivi.testing

use aivi.crypto

subject = randomUuid

@test
smoke = effect {
  u <- randomUuid
  _ <- testing.assert (length u == 36)
}
