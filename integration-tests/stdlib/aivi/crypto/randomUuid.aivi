@no_prelude
module integrationTests.stdlib.aivi.crypto.randomUuid

use aivi
use aivi.testing (assert)

use aivi.crypto

subject = randomUuid

@test
smoke = effect {
  u <- randomUuid
  _ <- assert (length u == 36)
}
