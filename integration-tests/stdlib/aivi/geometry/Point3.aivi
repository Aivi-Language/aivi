@no_prelude
module integrationTests.stdlib.aivi.geometry.Point3

use aivi
use aivi.testing
use aivi.geometry


touch : Point3 -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
