@no_prelude
module integrationTests.stdlib.aivi.geometry.Segment2

use aivi
use aivi.testing
use aivi.geometry


touch : Segment2 -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
