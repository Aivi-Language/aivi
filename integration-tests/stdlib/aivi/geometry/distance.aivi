@no_prelude
module integrationTests.stdlib.aivi.geometry.distance

use aivi
use aivi.testing (assert)

use aivi.geometry

subject = distance

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
