@no_prelude
module integrationTests.stdlib.aivi.geometry.Polygon

use aivi
use aivi.testing
use aivi.geometry

touch : Polygon -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
