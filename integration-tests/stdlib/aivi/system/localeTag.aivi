@no_prelude
module integrationTests.stdlib.aivi.system.localeTag

use aivi
use aivi.testing (assert)

use aivi.system

subject = localeTag

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
