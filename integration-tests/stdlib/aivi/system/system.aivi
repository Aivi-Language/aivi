@no_prelude
module integrationTests.stdlib.aivi.system.tests

use aivi
use aivi.testing
use aivi.system

@test
args_smoke = effect {
  pure args
  assert True
}

@test
env_smoke = effect {
  pure env
  assert True
}

@test
exit_smoke = effect {
  pure exit
  assert True
}

@test
localeTag_smoke = effect {
  pure localeTag
  assert True
}
