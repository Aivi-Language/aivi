@no_prelude
module integrationTests.stdlib.aivi.Result

use aivi
use aivi.testing
use aivi.logic

@test
constructors = effect {
  ok = Ok 1
  err = Err "nope"

  ok ?
    | Ok n  => assertEq n 1
    | Err _ => assert False

    err ?
      | Ok _  => assert False
      | Err e => assertEq e "nope"
}
