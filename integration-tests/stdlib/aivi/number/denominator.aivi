@no_prelude
module integrationTests.stdlib.aivi.number.denominator

use aivi
use aivi.testing (assert)

use aivi.number

subject = denominator

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
