@no_prelude
module integrationTests.stdlib.aivi.number.complex.i

use aivi
use aivi.testing

use aivi.number.complex

subject = i

@test
smoke = effect {
  _ <- testing.assert (i.re == 0.0)
  _ <- testing.assert (i.im == 1.0)
}
