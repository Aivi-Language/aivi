@no_prelude
module integrationTests.stdlib.aivi.number.complex.i

use aivi
use aivi.testing (assert)

use aivi.number.complex

subject = i

@test
smoke = effect {
  _ <- assert (i.re == 0.0)
  _ <- assert (i.im == 1.0)
}
