@no_prelude
module integrationTests.stdlib.aivi.number.complex.domain_Complex.Complex_op_x_3

use aivi
use aivi.testing (assert)

use aivi.number.complex

use aivi.number.complex (domain Complex)

left = { re: 1.0, im: 1.0 }
right = { re: 1.0, im: 1.0 }

subject = left * right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
