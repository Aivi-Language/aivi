@no_prelude
module integrationTests.stdlib.aivi.number.numerator

use aivi
use aivi.testing (assert)

use aivi.number

subject = numerator

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
