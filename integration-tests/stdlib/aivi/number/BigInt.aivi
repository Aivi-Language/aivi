@no_prelude
module integrationTests.stdlib.aivi.number.BigInt

use aivi
use aivi.testing (assert)

use aivi.number

Ref = BigInt

@test
smoke = effect {
  b = fromInt 123
  _ <- assert (toInt b == 123)
}
