@no_prelude
module integrationTests.stdlib.aivi.number.fromFloat

use aivi
use aivi.testing (assert)

use aivi.number

subject = fromFloat

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
