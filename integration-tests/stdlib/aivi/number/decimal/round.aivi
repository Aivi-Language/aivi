@no_prelude
module integrationTests.stdlib.aivi.number.decimal.round

use aivi
use aivi.testing (assert)

use aivi.number.decimal

subject = round

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
