@no_prelude
module integrationTests.stdlib.aivi.number.decimal.fromFloat

use aivi
use aivi.testing

use aivi.number.decimal

subject = fromFloat

@test
smoke = effect {
  d = fromFloat 1.23
  _ <- testing.assert (toFloat d == 1.23)
}
