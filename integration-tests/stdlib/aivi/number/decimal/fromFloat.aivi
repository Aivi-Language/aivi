@no_prelude
module integrationTests.stdlib.aivi.number.decimal.fromFloat

use aivi
use aivi.testing
use aivi.number.decimal

@test
smoke = effect {
  d = fromFloat 1.23
  _ <- assert (toFloat d == 1.23)
}
