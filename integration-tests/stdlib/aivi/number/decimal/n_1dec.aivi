@no_prelude
module integrationTests.stdlib.aivi.number.decimal.n_1dec

use aivi
use aivi.testing

use aivi.number.decimal

.14dec

@test
smoke = effect {
  d = 1.0dec
  _ <- testing.assert (toFloat d == 1.0)
}
