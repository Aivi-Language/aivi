@no_prelude
module integrationTests.stdlib.aivi.number.decimal.toFloat

use aivi
use aivi.testing (assert)

use aivi.number.decimal

subject = toFloat

@test
smoke = effect {
  d = fromFloat 123.456
  _ <- assert (toFloat d == 123.456)
}
