@no_prelude
module integrationTests.stdlib.aivi.number.decimal.toFloat

use aivi
use aivi.testing

use aivi.number.decimal

subject = toFloat

@test
smoke = effect {
  d = fromFloat 123.456
  _ <- testing.assert (toFloat d == 123.456)
}
