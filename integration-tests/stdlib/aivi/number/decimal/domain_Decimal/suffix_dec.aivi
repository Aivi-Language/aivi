@no_prelude
module integrationTests.stdlib.aivi.number.decimal.domain_Decimal.suffix_dec

use aivi
use aivi.testing (assert)

use aivi.number.decimal

use aivi.number.decimal (domain Decimal)

subject = 2dec

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
