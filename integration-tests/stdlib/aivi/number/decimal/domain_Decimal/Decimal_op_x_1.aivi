@no_prelude
module integrationTests.stdlib.aivi.number.decimal.domain_Decimal.Decimal_op_x_1

use aivi
use aivi.testing (assert)

use aivi.number.decimal

use aivi.number.decimal (domain Decimal)

left = Unit
right = Unit

subject = left + right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
