@no_prelude
module integrationTests.stdlib.aivi.number.Decimal

use aivi
use aivi.testing

use aivi.number

Ref = Decimal

@test
smoke = effect {
  d = fromFloat 12.34
  _ <- testing.assert (toFloat d == 12.34)
}
