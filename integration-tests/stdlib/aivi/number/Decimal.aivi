@no_prelude
module integrationTests.stdlib.aivi.number.Decimal

use aivi
use aivi.testing (assert)

use aivi.number

subject = Decimal

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
