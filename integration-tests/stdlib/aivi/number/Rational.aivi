@no_prelude
module integrationTests.stdlib.aivi.number.Rational

use aivi
use aivi.testing

use aivi.number



@test
smoke = effect {
  r = fromInts (fromInt 1) (fromInt 2)
  _ <- testing.assert (toInt (numerator r) == 1)
  _ <- testing.assert (toInt (denominator r) == 2)
}
