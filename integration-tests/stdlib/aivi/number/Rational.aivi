@no_prelude
module integrationTests.stdlib.aivi.number.Rational

use aivi
use aivi.testing
use aivi.number

touch : Rational -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
