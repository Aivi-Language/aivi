@no_prelude
module integrationTests.stdlib.aivi.number.Rational

use aivi
use aivi.testing (assert)

use aivi.number

subject = Rational

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
