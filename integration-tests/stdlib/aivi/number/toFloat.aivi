@no_prelude
module integrationTests.stdlib.aivi.number.toFloat

use aivi
use aivi.testing (assert)

use aivi.number

subject = toFloat

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
