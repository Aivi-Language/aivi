@no_prelude
module integrationTests.stdlib.aivi.number.quaternion.fromAxisAngle

use aivi
use aivi.testing

use aivi.number.quaternion



@test
smoke = effect {
  q = fromAxisAngle { x: 1.0, y: 0.0, z: 0.0 } 90.0
  _ <- testing.assert (q.w > 0.707) // approx cos(45)
}
