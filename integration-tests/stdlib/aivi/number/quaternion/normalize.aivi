@no_prelude
module integrationTests.stdlib.aivi.number.quaternion.normalize

use aivi
use aivi.testing

use aivi.number.quaternion



@test
smoke = effect {
  q = { w: 2.0, x: 0.0, y: 0.0, z: 0.0 }
  n = normalize q
  _ <- assert (n.w == 1.0)
}
