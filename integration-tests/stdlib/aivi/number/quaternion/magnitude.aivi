@no_prelude
module integrationTests.stdlib.aivi.number.quaternion.magnitude

use aivi
use aivi.testing

use aivi.number.quaternion



@test
smoke = effect {
  q = { w: 1.0, x: 0.0, y: 0.0, z: 0.0 }
  _ <- assert (magnitude q == 1.0)
}
