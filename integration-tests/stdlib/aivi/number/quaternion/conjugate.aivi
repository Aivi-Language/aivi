@no_prelude
module integrationTests.stdlib.aivi.number.quaternion.conjugate

use aivi
use aivi.testing (assert)

use aivi.number.quaternion

subject = conjugate

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
