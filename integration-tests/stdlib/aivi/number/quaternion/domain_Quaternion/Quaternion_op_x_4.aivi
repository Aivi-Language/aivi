@no_prelude
module integrationTests.stdlib.aivi.number.quaternion.domain_Quaternion.Quaternion_op_x_4

use aivi
use aivi.testing (assert)

use aivi.number.quaternion

use aivi.number.quaternion (domain Quaternion)

left = { w: 1.0, x: 1.0, y: 1.0, z: 1.0 }
right = 1.0

subject = left / right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
