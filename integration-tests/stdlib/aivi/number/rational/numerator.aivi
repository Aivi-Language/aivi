@no_prelude
module integrationTests.stdlib.aivi.number.rational.numerator

use aivi
use aivi.testing (assert)

use aivi.number.rational

subject = numerator

@test
smoke = effect {
  r = fromInts (fromInt 1) (fromInt 2)
  _ <- assert (toInt (numerator r) == 1)
}
