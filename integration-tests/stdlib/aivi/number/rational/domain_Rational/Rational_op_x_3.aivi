@no_prelude
module integrationTests.stdlib.aivi.number.rational.domain_Rational.Rational_op_x_3

use aivi
use aivi.testing (assert)

use aivi.number.rational

use aivi.number.rational (domain Rational)

left = Unit
right = Unit

subject = left * right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
