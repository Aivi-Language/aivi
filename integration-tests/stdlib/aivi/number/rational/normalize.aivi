@no_prelude
module integrationTests.stdlib.aivi.number.rational.normalize

use aivi
use aivi.testing
use aivi.number.rational

touch : Rational -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- pure normalize
  _ <- assertEq (1 + 1) 2
}
