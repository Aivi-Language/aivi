@no_prelude
module integrationTests.stdlib.aivi.number.rational.denominator

use aivi
use aivi.testing (assert)

use aivi.number.rational

subject = denominator

@test
smoke = effect {
  r = fromInts (fromInt 1) (fromInt 2)
  _ <- assert (toInt (denominator r) == 2)
}
