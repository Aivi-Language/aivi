@no_prelude
module integrationTests.stdlib.aivi.number.Complex

use aivi
use aivi.testing (assert)

use aivi.number

subject = Complex

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
