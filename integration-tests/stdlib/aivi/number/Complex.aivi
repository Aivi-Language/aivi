@no_prelude
module integrationTests.stdlib.aivi.number.Complex

use aivi
use aivi.testing (assert)

use aivi.number

Ref = Complex

@test
smoke = effect {
  z = { re: 1.0, im: 2.0 }
  _ <- assert (z.re == 1.0)
}
