@no_prelude
module integrationTests.stdlib.aivi.number.normalize

use aivi
use aivi.testing
use aivi.number

@test
smoke = effect {
  r = normalize (fromInts (fromInt 2) (fromInt 4))
  _ <- assert (toInt (numerator r) == 1)
  _ <- assert (toInt (denominator r) == 2)
}
