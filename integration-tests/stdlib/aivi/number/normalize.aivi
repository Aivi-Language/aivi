@no_prelude
module integrationTests.stdlib.aivi.number.normalize

use aivi
use aivi.testing (assert)

use aivi.number

subject = normalize

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
