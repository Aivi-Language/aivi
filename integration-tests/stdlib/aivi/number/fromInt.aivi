@no_prelude
module integrationTests.stdlib.aivi.number.fromInt

use aivi
use aivi.testing (assert)

use aivi.number

subject = fromInt

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
