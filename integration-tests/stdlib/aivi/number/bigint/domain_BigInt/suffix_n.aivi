@no_prelude
module integrationTests.stdlib.aivi.number.bigint.domain_BigInt.suffix_n

use aivi
use aivi.testing (assert)

use aivi.number.bigint

use aivi.number.bigint (domain BigInt)

subject = 2n

@test
smoke = effect {
  b = 10n
  _ <- assert (toInt b == 10)
}
