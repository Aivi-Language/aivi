@no_prelude
module integrationTests.stdlib.aivi.number.bigint.fromInt

use aivi
use aivi.testing

use aivi.number.bigint

subject = fromInt

@test
smoke = effect {
  b = fromInt 10
  _ <- testing.assert (toInt b == 10)
}
