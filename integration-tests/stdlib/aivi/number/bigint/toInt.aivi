@no_prelude
module integrationTests.stdlib.aivi.number.bigint.toInt

use aivi
use aivi.testing (assert)

use aivi.number.bigint

subject = toInt

@test
smoke = effect {
  b = fromInt 123
  _ <- assert (toInt b == 123)
}
