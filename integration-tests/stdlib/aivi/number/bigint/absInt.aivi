@no_prelude
module integrationTests.stdlib.aivi.number.bigint.absInt

use aivi
use aivi.testing
use aivi.number.bigint

@test
smoke = effect {
  b = absInt (fromInt (-5))
  _ <- assert (toInt b == 5)
}
