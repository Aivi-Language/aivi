@no_prelude
module integrationTests.stdlib.aivi.number.bigint.absInt

use aivi
use aivi.testing

use aivi.number.bigint

subject = absInt

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
