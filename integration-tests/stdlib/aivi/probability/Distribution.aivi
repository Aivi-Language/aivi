@no_prelude
module integrationTests.stdlib.aivi.probability.Distribution

use aivi
use aivi.testing (assert)

use aivi.probability

subject = Distribution

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
