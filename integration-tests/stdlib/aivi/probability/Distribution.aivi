@no_prelude
module integrationTests.stdlib.aivi.probability.Distribution

use aivi
use aivi.testing
use aivi.probability


touch : Distribution -> Unit
touch = _ => Unit

@test
smoke = effect {
  _ <- pure touch
  _ <- assertEq (1 + 1) 2
}
