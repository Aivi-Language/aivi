@no_prelude
module integrationTests.stdlib.aivi.probability.clamp

use aivi
use aivi.testing (assert)

use aivi.probability

subject = clamp

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
