@no_prelude
module integrationTests.stdlib.aivi.probability.domain_Probability.Probability_op_x_3

use aivi
use aivi.testing (assert)

use aivi.probability

use aivi.probability (domain Probability)

left = Unit
right = Unit

subject = left * right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
