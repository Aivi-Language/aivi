@no_prelude
module integrationTests.stdlib.aivi.probability.expectation

use aivi
use aivi.testing (assert)

use aivi.probability

subject = expectation

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
