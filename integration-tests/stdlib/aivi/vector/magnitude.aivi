@no_prelude
module integrationTests.stdlib.aivi.vector.magnitude

use aivi
use aivi.testing
use aivi.vector

@test
smoke = effect {
  v = { x: 3.0, y: 4.0 }
  _ <- assert (abs (magnitude v - 5.0) < 0.0001)
}
