@no_prelude
module integrationTests.stdlib.aivi.vector.magnitude

use aivi
use aivi.testing (assert)

use aivi.vector

subject = magnitude

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
