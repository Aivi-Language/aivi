@no_prelude
module integrationTests.stdlib.aivi.vector.normalize

use aivi
use aivi.testing (assert)

use aivi.vector

subject = normalize

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
