@no_prelude
module integrationTests.stdlib.aivi.vector.Vec2

use aivi
use aivi.testing
use aivi.vector

@test
smoke = effect {
  v : Vec2
  v = { x: 3.0, y: 4.0 }
  _ <- assert (abs (magnitude v - 5.0) < 0.0001)
  _ <- assert (dot v v == 25.0)
}
