@no_prelude
module integrationTests.stdlib.aivi.vector.Vec4

use aivi
use aivi.testing
use aivi.vector

@test
smoke = effect {
  v : Vec4
  v = { x: 1.0, y: 2.0, z: 3.0, w: 4.0 }
  _ <- assert (v.w == 4.0)
}
