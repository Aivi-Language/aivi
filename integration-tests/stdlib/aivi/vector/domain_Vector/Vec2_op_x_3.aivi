@no_prelude
module integrationTests.stdlib.aivi.vector.domain_Vector.Vec2_op_x_3

use aivi
use aivi.testing (assert)

use aivi.vector

use aivi.vector (domain Vector)

left = { x: 1.0, y: 1.0 }
right = 1.0

subject = left * right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
