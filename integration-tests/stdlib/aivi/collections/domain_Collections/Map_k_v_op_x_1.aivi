@no_prelude
module integrationTests.stdlib.aivi.collections.domain_Collections.Map_k_v_op_x_1

use aivi
use aivi.testing (assert)

use aivi.collections

use aivi.collections (domain Collections)

left = Map.insert "a" 1 Map.empty
right = Map.insert "a" 1 Map.empty

subject = left ++ right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
