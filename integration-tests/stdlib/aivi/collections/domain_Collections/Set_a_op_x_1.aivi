@no_prelude
module integrationTests.stdlib.aivi.collections.domain_Collections.Set_a_op_x_1

use aivi
use aivi.testing (assert)

use aivi.collections

use aivi.collections (domain Collections)

left = Set.insert 1 Set.empty
right = Set.insert 1 Set.empty

subject = left ++ right

@test
smoke = effect {
  _ <- pure subject
  _ <- assert True
}
