@no_prelude
module integrationTests.stdlib.aivi.collections.Map

use aivi
use aivi.testing
use aivi.collections

@test
smoke = effect {
  m0 = Map.empty
  m1 = Map.insert 1 "one" m0

  _ <- assert (Map.size m0 == 0)
  _ <- assert (Map.size m1 == 1)
  _ <- assert (Map.get 1 m1 == Some "one")
}
