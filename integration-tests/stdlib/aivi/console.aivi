@no_prelude
module integrationTests.stdlib.aivi.console

use aivi
use aivi.testing
use aivi

@test
smoke = effect {
  _ <- print "console test"
  _ <- println "console newline"
  _ <- assertEq (1 + 1) 2
}
