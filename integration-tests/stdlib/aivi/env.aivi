@no_prelude
module integrationTests.stdlib.aivi.env

use aivi
use aivi.testing
use aivi.env
use aivi.text

@test
smoke = effect {
  src = env.get "PATH"
  _ <- src ?
    | Some path => assert (text.isEmpty path == False)
    | None      => assert True
}
