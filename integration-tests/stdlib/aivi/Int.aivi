@no_prelude
module integrationTests.stdlib.aivi.Int

use aivi
use aivi.testing
use aivi

@test
smoke = effect {
  assert (1 + 2 == 3)
  assert (2 * 3 == 6)
  assert (4 / 2 == 2)
  assert (1 < 2)
  assert (2 > 1)
  assert (1 <= 1)
  assert (1 >= 1)
  assert (Int.min 1 2 == 1)
  assert (Int.max 1 2 == 2)
}
