@no_prelude
module integrationTests.stdlib.aivi.file

use aivi
use aivi.testing (assert)

use aivi

subject = file

@test
smoke = effect {
  // read is a source
  src = file.read "README.md"
  _ <- assert True
}
