// EXPECT-ERROR: E3000
@no_prelude
module integrationTests.compileFail.overlappingInstances

use aivi

class Show2 A = {
  show2: A -> Text
}

instance Show2 Int = {
  show2 = x => "a"
}

instance Show2 Int = {
  show2 = x => "b"
}

value = show2 42
