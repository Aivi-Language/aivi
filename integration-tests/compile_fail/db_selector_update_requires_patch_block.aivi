// EXPECT-ERROR: database selector updates accept only a patch block
@no_prelude
module integrationTests.compileFail.dbSelectorUpdateRequiresPatchBlock

use aivi
use aivi.database

users : Table { id: Int, name: Text }
users = table "users"[]

bad = users[id == 1] <| 1
