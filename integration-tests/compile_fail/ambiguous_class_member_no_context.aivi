// EXPECT-ERROR: E3000
@no_prelude
module integrationTests.compileFail.ambiguousClassMemberNoContext

use aivi
use aivi.prelude
use aivi.logic

// `empty` without type context should fail because
// multiple Monoid instances exist (List, Map).
ambiguous = empty
