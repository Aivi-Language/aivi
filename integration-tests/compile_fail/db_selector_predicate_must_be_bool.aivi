// EXPECT-ERROR: expected `Bool`, found `Int`
@no_prelude
module integrationTests.compileFail.dbSelectorPredicateMustBeBool

use aivi
use aivi.database

users : Table { id: Int }
users = table "users" [
  {
    name: "id"
    type: IntType
    constraints: []
    default: None
  }
]

bad = rows users[(u => u.id)]
