// EXPECT-ERROR: applicative bindings must be independent
@no_prelude
module integrationTests.compileFail.applicativeDoDependentBind

use aivi
use aivi.prelude

bad = do Applicative {
  x <- Some 1
  y <- Some (x + 1)
  x + y
}
