// EXPECT-ERROR: E3000
module integrationTests.compileFail.missingRecordField

MyProps = { label: Text, onChange: Text -> Effect Unit }

render : MyProps -> Text
render = props => props.label

// `onChange` has no default — calling with a partial record is an error
bad = render { label: "hello" }
