// EXPECT-ERROR: unknown name 'applyDelta'
@no_prelude
module integrationTests.compileFail.legacyApplyDeltaRemoved

use aivi
use aivi.database

User = { id: Int }

users : Relation User
users = relation "users" [
  { name: "id", type: IntType, constraints: [NotNull], default: None }
] []

bad = applyDelta users { id: 1 }
