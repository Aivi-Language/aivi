// EXPECT-ERROR: E3000
@no_prelude
module integrationTests.compileFail.whenInGenericDoBlock

use aivi

// `when` is only valid in `do Effect`, not generic `do M` blocks.
bad = do Option {
  when True <- pure Unit
  pure Unit
}
