// EXPECT-ERROR: unknown name
@no_prelude
module integrationTests.compileFail.preludeNotImported

use aivi.prelude

// The prelude restores core names, but focused text helpers still require `aivi.text`.
bad = length "hello"
