@no_prelude
module integrationTests.syntax.modules.useMultilineImports

use aivi.text (
  length
  toUpper
)
use aivi.testing

@test "multiline use without commas"
multilineUseWorks = {
  assertEq (length "hello") 5
  assertEq (toUpper "hi") "HI"
}
