@no_prelude
module integrationTests.syntax.types.opaqueTypes.consumer

use aivi
use aivi.testing

use integrationTests.syntax.types.opaqueTypes.definer (Wrapper, make, value)

@test "opaque types: construct via exported function"
opaqueConstruct = {
  w = make 42
  assertEq (value w) 42
}
