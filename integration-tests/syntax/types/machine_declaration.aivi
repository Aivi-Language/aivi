@no_prelude
module integrationTests.syntax.types.machineDeclaration

machine Door = {
  state Closed =
    on Open => Opened
  state Opened =
    on Close => Closed
    on Lock => Locked
  state Locked =
    on Unlock => Closed
}

machine TrafficLight = {
  state Red =
    on Next => Green
  state Green =
    on Next => Yellow
  state Yellow =
    on Next => Red
}

