@no_prelude
module integrationTests.syntax.bindings.basic

use aivi
use aivi.testing

x = 1
y = if x < 2 then 3 else 4
pair = (x, y)

@test
basicBindings = do Effect {
  assertEq x 1
  assertEq y 3
  assertEq pair (1, 3)
}
