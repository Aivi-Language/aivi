@no_prelude
module integrationTests.syntax.bindings.recursion

use aivi
use aivi.testing

sum = xs => xs ?
  | []           => 0
  | [x, ...rest] => x + sum rest

@test
recursionWorks = effect {
  assertEq (sum[1, 2, 3]) 6
}
