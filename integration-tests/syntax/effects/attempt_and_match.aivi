@no_prelude
module integrationTests.syntax.effects.attemptAndMatch

use aivi
use aivi.testing

@test "attempt exposes failure to branch matching"
attemptExposesFailureToBranchMatching =
  attempt (fail "nope")
    |> attemptResult =>
         assertErr attemptResult
           |> err => assertEq err "nope"
