@no_prelude
module integrationTests.syntax.generators.basicYield

use aivi
use aivi.testing
use aivi.generator

gen = generate {
  yield 1
  yield 2
  yield 3
}

@test "generate/yield to list"
generatorsExist = do Effect {
  assert (toList gen == [1, 2, 3])
}
