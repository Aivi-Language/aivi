module examples.stdlib_crypto
export main

use aivi.console
use aivi.crypto

main : Effect Text Unit
main = effect {
  hash = sha256 "aivi"
  print "sha256: {hash}"

  uuid <- randomUuid
  print "uuid: {uuid}"

  bytes <- randomBytes 16
  print "bytes: {bytes}"
}
