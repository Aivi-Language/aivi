@no_prelude
module examples.compiler.app
export run

use examples.compiler.math as math
use examples.compiler.stats as stats
use aivi.testing
use aivi


run = [1, 2, 3, 4]
  |> stats.mean
  |> math.add (math.square 2)

__test_sentinel = 0

@test
smoke = effect {
  _ <- assert (run == 6.5)
}
