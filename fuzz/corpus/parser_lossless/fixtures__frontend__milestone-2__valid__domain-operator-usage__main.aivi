domain Duration over Int
    literal ms : Int -> Duration
    type Duration -> Duration -> Duration
    (+)
    type Duration -> Int
    value

val total : Duration = 10ms + 5ms
val raw : Int = value total
