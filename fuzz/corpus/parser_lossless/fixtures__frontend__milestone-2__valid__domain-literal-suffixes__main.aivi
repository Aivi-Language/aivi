domain Duration over Int
    literal ms : Int -> Duration
    literal sec : Int -> Duration
    type Duration -> Duration -> Duration
    (+)
    type Duration -> Int
    value

val delay : Duration = 250ms
val total = 250ms + 3sec

export delay
