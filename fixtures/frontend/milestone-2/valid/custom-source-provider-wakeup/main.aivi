fun step:Int value:Int current:Int =>
    value

provider custom.feed
    wakeup: providerTrigger

@source custom.feed
sig retryEvents : Signal Int

sig retried : Signal Int =
    retryEvents
     |> scan 0 step
