domain Duration over Int = {
    suffix ms
    type ms : Int
    ms = value => Duration value
    type (+) : Duration -> Duration -> Duration
    type toInt : Duration -> Int
    toInt = duration => duration
}

value total : Duration = 10ms + 5ms
value raw : Int = toInt total
