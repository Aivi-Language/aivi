domain Duration over Int = {
    suffix ms
    type ms : Int
    ms = value => Duration value
    type make : Int -> Duration
    type millis : Duration -> Int
    millis = duration => duration
}

domain Count over Int = {
    type make : Int -> Count
}

domain NonEmpty A over List A = {
    type singleton : A -> NonEmpty A
    type head : NonEmpty A -> A
}

value delay : Duration = make 250
value delayMillis : Int = millis delay
value literalMillis : Int = millis 250ms
value first : Int = head (singleton 1)

export first
